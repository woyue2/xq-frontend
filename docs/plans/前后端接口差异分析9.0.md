# 前后端接口差异分析 9.0

> 本文档记录 user-me（用户信息管理）模块的前后端接口字段一致性分析结果。

## 模块健康度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 接口完整性 | ⚠️ 中等 | 缺少获取用户信息专用方法，需复用 updateProfile 调用方式 |
| 类型安全性 | ⚠️ 中等 | PATCH /users/me 响应缺少 expiresAt 和 classHours |
| 参数字段一致性 | ✅ 良好 | 参数命名和类型基本一致 |
| 总体健康度 | ⚠️ 中等 | 响应结构可优化 |

## 发现的问题

### 问题 91：PATCH /users/me 响应缺少 expiresAt 和 classHours 字段

**优先级**: 高

**问题描述**:
后端 `GET /users/me` 接口返回用户信息时包含 `expiresAt` 和 `classHours` 字段，但 `PATCH /users/me` 接口更新用户信息后返回的响应中缺少这两个字段。这导致前端在更新用户信息后无法立即获取最新的课时状态。

**后端现状**:
```typescript
// backend/src/routes/user-me.routes.ts
// GET /users/me 返回
return res.json({
  data: {
    id: user.id,
    phone: user.phone,
    name: user.name ?? undefined,
    nickname: user.nickname,
    avatar: user.avatar ?? undefined,
    role: user.role,
    grade: user.grade ?? undefined,
    age: user.age ?? undefined,
    school: user.school ?? undefined,
    expiresAt: user.expiresAt ?? undefined,
    classHours  // ✅ 返回课时状态
  }
});

// PATCH /users/me 返回
return res.json({
  data: {
    id: updatedUser.id,
    phone: updatedUser.phone,
    name: updatedUser.name ?? undefined,
    nickname: updatedUser.nickname,
    avatar: updatedUser.avatar,
    role: updatedUser.role,
    grade: updatedUser.grade,
    age: updatedUser.age,
    school: updatedUser.school
    // ❌ 缺少 expiresAt 和 classHours
  }
});
```

**前端现状**:
```typescript
// src/services/api.ts
export const userService = {
    updateProfile: async (data: UpdateProfilePayload) => {
        const { data: res } = await api.patch<ApiResponse<User>>('/users/me', data);
        return res.data;
    }
};
```

**修复建议**:
```typescript
// 后端 PATCH /users/me 响应补充 expiresAt 和 classHours
userMeRouter.patch(
  '/',
  async (req: AuthenticatedRequest, res: Response, next: NextFunction) => {
    // ... 更新逻辑

    const updatedUser = await userService.updateProfile(req.user.id, data);

    // 获取更新后的课时状态
    let classHours: Awaited<ReturnType<typeof classHoursService.getUserClassHours>> | null = null;
    try {
      classHours = await classHoursService.getUserClassHours(updatedUser.id);
    } catch {
      classHours = null;
    }

    return res.json({
      code: 200,
      message: 'success',
      data: {
        id: updatedUser.id,
        phone: updatedUser.phone,
        name: updatedUser.name ?? undefined,
        nickname: updatedUser.nickname,
        avatar: updatedUser.avatar ?? undefined,
        role: updatedUser.role,
        grade: updatedUser.grade ?? undefined,
        age: updatedUser.age ?? undefined,
        school: updatedUser.school ?? undefined,
        expiresAt: updatedUser.expiresAt ?? undefined,
        classHours  // ✅ 添加
      },
      timestamp: Date.now()
    });
  }
);
```

---

### 问题 92：前端缺少 getUserInfo 专用方法

**优先级**: 中

**问题描述**:
前端 `profileService` 只提供了 `getMyLikes` 和 `getMyFavorites` 方法，没有专门获取用户基本信息的 `getUserInfo` 方法。前端在需要获取当前用户信息时，需要通过 `updateProfile` 的调用方式或自行调用 API。

**前端现状**:
```typescript
// src/services/api.ts
export const profileService = {
    // 缺少 getUserInfo 方法
    getMyLikes: async (params?: { page?: number; pageSize?: number }) => { /* ... */ },
    getMyFavorites: async (params?: { page?: number; pageSize?: number }) => { /* ... */ }
};

// src/services/api.ts
export const userService = {
    updateProfile: async (data: UpdateProfilePayload) => {
        const { data: res } = await api.patch<ApiResponse<User>>('/users/me', data);
        return res.data;
    }
};
```

**修复建议**:
```typescript
// 前端添加 getUserInfo 方法
export const userService = {
    getUserInfo: async () => {
        const { data: res } = await api.get<ApiResponse<User>>('/users/me');
        return res.data;
    },
    updateProfile: async (data: UpdateProfilePayload) => {
        const { data: res } = await api.patch<ApiResponse<User>>('/users/me', data);
        return res.data;
    }
};
```

---

### 问题 93：PATCH /users/me 后端年龄验证返回错误格式不一致

**优先级**: 中

**问题描述**:
后端 `PATCH /users/me` 接口对 `age` 字段的验证失败时，返回的错误格式与其他字段不一致。age 验证失败返回结构化错误，其他字段验证失败可能返回简单错误。

**后端现状**:
```typescript
// backend/src/routes/user-me.routes.ts
if (key === 'age') {
  if (value === null || value === undefined || value === '') {
    return acc;
  }
  const num = typeof value === 'number' ? value : Number(value);
  if (!Number.isInteger(num) || num < 0 || num > 120) {
    throw new AppError(
      400,
      'INVALID_PARAMS',
      '年龄格式不正确',
      {
        errors: [
          { field: 'age', message: 'age 必须为 0~120 的整数' }
        ]
      }
    );
  }
  acc.age = num;
  return acc;
}

// 其他字段验证
if (
  (key === 'name' ||
    key === 'nickname' ||
    key === 'avatar' ||
    key === 'grade' ||
    key === 'school') &&
  value !== null &&
  value !== undefined
) {
  if (typeof value !== 'string') {
    throw new AppError(
      400,
      'INVALID_PARAMS',
      '参数类型错误',
      {
        errors: [
          { field: key, message: `${key} 必须为字符串` }
        ]
      }
    );
  }
  acc[key] = value;
}
```

**问题分析**:
age 字段的验证消息是"年龄格式不正确"，而其他字段是"参数类型错误"。虽然 age 的消息更语义化，但与其他字段的格式不统一。

**修复建议**:
```typescript
// 统一错误消息格式
if (!Number.isInteger(num) || num < 0 || num > 120) {
  throw new AppError(
    400,
    'INVALID_PARAMS',
    'age 必须在 0~120 之间',
    {
      errors: [
        { field: 'age', message: 'age 必须在 0~120 之间' }
      ]
    }
  );
}
```

---

### 问题 94：后端 PATCH /users/me 缺少 nickname 长度限制

**优先级**: 低

**问题描述**:
后端 `PATCH /users/me` 接口对 `nickname` 字段没有长度限制验证，虽然前端类型定义中没有明确限制，但业务上通常需要限制昵称长度（如 2-20 字符）。

**后端现状**:
```typescript
// backend/src/routes/user-me.routes.ts
const allowedFields = ['name', 'nickname', 'avatar', 'grade', 'age', 'school'];
// nickname 没有长度验证
if (
  (key === 'name' ||
    key === 'nickname' ||  // 没有长度限制
    // ...
  ) &&
  value !== null &&
  value !== undefined
) {
  if (typeof value !== 'string') {
    throw new AppError(/* ... */);
  }
  acc[key] = value;
}
```

**前端现状**:
```typescript
// src/types/api.ts
export interface UpdateProfilePayload {
    name?: string;
    nickname?: string;  // 没有长度限制
    avatar?: string;
    grade?: string;
    age?: number;
    school?: string;
}
```

**修复建议**:
```typescript
// 后端添加 nickname 长度验证
if (key === 'nickname') {
  if (typeof value !== 'string') {
    throw new AppError(
      400,
      'INVALID_PARAMS',
      '参数类型错误',
      { errors: [{ field: 'nickname', message: 'nickname 必须为字符串' }] }
    );
  }
  if (value.length < 2 || value.length > 20) {
    throw new AppError(
      400,
      'INVALID_PARAMS',
      '昵称长度必须在 2~20 个字符之间',
      { errors: [{ field: 'nickname', message: 'nickname 长度必须在 2~20 之间' }] }
    );
  }
  acc.nickname = value;
}
```

---

### 问题 95：GET /users/me/likes 和 GET /users/me/favorites 缺少响应类型定义

**优先级**: 低

**问题描述**:
前端 `profileService` 的 `getMyLikes` 和 `getMyFavorites` 方法虽然正确使用了 `ApiResponse` 类型，但返回的 `data.data`（即 `list` 和 `pagination`）没有专门的类型封装。

**前端现状**:
```typescript
// src/services/api.ts
export const profileService = {
    getMyLikes: async (params?: { page?: number; pageSize?: number }) => {
        const { data } = await api.get<
            ApiResponse<{
                list: MyLikedQuestion[];
                pagination: {
                    page: number;
                    pageSize: number;
                    total: number;
                    totalPages: number;
                };
            }>
        >('/users/me/likes', { params });
        return data.data;
    },
    getMyFavorites: async (params?: { page?: number; pageSize?: number }) => {
        const { data } = await api.get<
            ApiResponse<{
                list: MyFavoritedQuestion[];
                pagination: {
                    page: number;
                    pageSize: number;
                    total: number;
                    totalPages: number;
                };
            }
        >('/users/me/favorites', { params });
        return data.data;
    }
};
```

**修复建议**:
```typescript
// 前端添加分页响应类型
export interface PaginatedResponse<T> {
    list: T[];
    pagination: {
        page: number;
        pageSize: number;
        total: number;
        totalPages: number;
    };
}

// 使用
export const profileService = {
    getMyLikes: async (params?: { page?: number; pageSize?: number }) => {
        const { data } = await api.get<
            ApiResponse<PaginatedResponse<MyLikedQuestion>>
        >('/users/me/likes', { params });
        return data.data;
    },
    getMyFavorites: async (params?: { page?: number; pageSize?: number }) => {
        const { data } = await api.get<
            ApiResponse<PaginatedResponse<MyFavoritedQuestion>>
        >('/users/me/favorites', { params });
        return data.data;
    }
};
```

---

## 字段映射表

### GET /users/me 响应字段

| 字段 | 后端返回 | 前端类型 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| id | ✅ | ✅ | ✅ | - |
| phone | ✅ | ✅ | ✅ | - |
| name | ✅ | ✅ | ✅ | 可选字段 |
| nickname | ✅ | ✅ | ✅ | - |
| avatar | ✅ | ✅ | ✅ | 可选字段 |
| role | ✅ | ✅ | ✅ | - |
| grade | ✅ | ✅ | ✅ | 可选字段 |
| age | ✅ | ✅ | ✅ | 可选字段 |
| school | ✅ | ✅ | ✅ | 可选字段 |
| expiresAt | ✅ | ✅ | ✅ | - |
| classHours | ✅ | ✅ | ✅ | - |
| isValidMember | ❌ | ✅ | ❌ | 前端有定义，后端未返回 |
| permissions | ❌ | ✅ | ❌ | 前端有定义，后端未返回 |

### PATCH /users/me 请求字段

| 字段 | 后端允许 | 前端类型 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| name | ✅ | ✅ | ✅ | 可选 |
| nickname | ✅ | ✅ | ✅ | 可选，无长度限制 |
| avatar | ✅ | ✅ | ✅ | 可选 |
| grade | ✅ | ✅ | ✅ | 可选 |
| age | ✅ | ✅ | ✅ | 可选，0~120 整数 |
| school | ✅ | ✅ | ✅ | 可选 |

### PATCH /users/me 响应字段

| 字段 | 后端返回 | 前端期望 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| id | ✅ | ✅ | ✅ | - |
| phone | ✅ | ✅ | ✅ | - |
| name | ✅ | ✅ | ✅ | - |
| nickname | ✅ | ✅ | ✅ | - |
| avatar | ✅ | ✅ | ✅ | - |
| role | ✅ | ✅ | ✅ | - |
| grade | ✅ | ✅ | ✅ | - |
| age | ✅ | ✅ | ✅ | - |
| school | ✅ | ✅ | ✅ | - |
| expiresAt | ❌ | ✅ | ❌ | GET 返回，PATCH 未返回 |
| classHours | ❌ | ✅ | ❌ | GET 返回，PATCH 未返回 |

### GET /users/me/likes 响应字段

| 字段 | 后端返回 | 前端类型 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| id | ✅ | ✅ | ✅ | 问题 ID |
| title | ✅ | ✅ | ✅ | - |
| content | ✅ | ✅ | ✅ | 可选 |
| authorName | ✅ | ✅ | ✅ | - |
| authorAvatar | ✅ | ✅ | ✅ | 可选 |
| likes | ✅ | ✅ | ✅ | - |
| favorites | ✅ | ✅ | ✅ | - |
| answers | ✅ | ✅ | ✅ | - |
| createdAt | ✅ | ✅ | ✅ | 问题创建时间 |
| likedAt | ✅ | ✅ | ✅ | 点赞时间 |

### GET /users/me/favorites 响应字段

| 字段 | 后端返回 | 前端类型 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| id | ✅ | ✅ | ✅ | 问题 ID |
| title | ✅ | ✅ | ✅ | - |
| content | ✅ | ✅ | ✅ | 可选 |
| authorName | ✅ | ✅ | ✅ | - |
| authorAvatar | ✅ | ✅ | ✅ | 可选 |
| likes | ✅ | ✅ | ✅ | - |
| favorites | ✅ | ✅ | ✅ | - |
| answers | ✅ | ✅ | ✅ | - |
| createdAt | ✅ | ✅ | ✅ | 问题创建时间 |
| favoritedAt | ✅ | ✅ | ✅ | 收藏时间 |

---

## 优化建议

### 短期优化（高优先级）

1. **PATCH /users/me 响应补充 expiresAt 和 classHours**: 确保更新用户信息后能获取最新课时状态
2. **添加前端 getUserInfo 方法**: 完善 userService 的 API 封装

### 中期优化（中优先级）

1. **统一错误消息格式**: age 字段验证失败消息与其他字段保持一致
2. **添加 nickname 长度限制**: 限制昵称 2-20 字符

### 长期优化（低优先级）

1. **添加 PaginatedResponse 泛型类型**: 统一分页响应结构
2. **补充 isValidMember 和 permissions 计算逻辑**: 或者从 User 类型中移除

---

## 总结

user-me（用户信息管理）模块整体功能完整，但存在以下问题：
1. `PATCH /users/me` 响应缺少 `expiresAt` 和 `classHours` 字段
2. 前端缺少 `getUserInfo` 专用方法
3. `PATCH /users/me` 错误消息格式不统一
4. `nickname` 字段缺少长度限制

建议按优先级逐步修复响应结构和类型定义，确保前后端数据结构一致。

---

# question（问题）模块分析

## 模块健康度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 接口完整性 | ⚠️ 中等 | 缺少问题详情接口的部分字段返回 |
| 类型安全性 | ⚠️ 中等 | GET /questions/:id 缺少 aiResult 和 aiAudit |
| 参数字段一致性 | ✅ 良好 | 参数命名和类型基本一致 |
| 总体健康度 | ⚠️ 中等 | 创建和详情接口返回字段不一致 |

## 发现的问题

### 问题 96：GET /questions/:id 缺少 aiResult 字段

**优先级**: 高

**问题描述**:
前端 `Question` 类型定义了 `aiResult?: string` 字段，但后端 `GET /questions/:id` 接口返回的数据中没有包含 `aiResult` 字段。这导致前端在查看问题详情时无法获取 AI 审核结果文本。

**后端现状**:
```typescript
// backend/src/services/question.service.ts - getById 方法
return {
  id: q.id,
  title: q.title,
  content: q.content,
  images: q.images ?? [],
  audioUrl: null,
  tags: q.tags,
  difficulty: q.difficulty,
  authorId: q.authorId,
  authorName: q.authorName,
  authorAvatar: q.authorAvatar ?? undefined,
  authorRole: author?.role,
  isGoodQuestion: q.isGoodQuestion,
  isPinned: q.isPinned,
  score: q.score ?? undefined,
  understoodCount: q.understoodCount,
  notUnderstoodCount: q.notUnderstoodCount,
  likes: q.likes,
  favorites: q.favorites,
  comments: q.comments,
  answers: q.answers,
  status: q.status,
  createdAt: q.createdAt,
  isLiked: false,
  isFavorited: false
  // ❌ 缺少 aiResult
};
```

**前端现状**:
```typescript
// src/types/index.ts
export interface Question {
  id: string;
  title: string;
  // ...
  aiResult?: string;  // ✅ 已定义
  aiAudit?: {
    safe: boolean;
    reason?: string;
    qualitySuggestion?: string;
  };
  // ...
}
```

**修复建议**:
```typescript
// 后端 getById 方法补充 aiResult
return {
  id: q.id,
  title: q.title,
  content: q.content,
  images: q.images ?? [],
  audioUrl: null,
  tags: q.tags,
  difficulty: q.difficulty,
  authorId: q.authorId,
  authorName: q.authorName,
  authorAvatar: q.authorAvatar ?? undefined,
  authorRole: author?.role,
  isGoodQuestion: q.isGoodQuestion,
  isPinned: q.isPinned,
  score: q.score ?? undefined,
  understoodCount: q.understoodCount,
  notUnderstoodCount: q.notUnderstoodCount,
  likes: q.likes,
  favorites: q.favorites,
  comments: q.comments,
  answers: q.answers,
  status: q.status,
  createdAt: q.createdAt,
  aiResult: q.aiResult ?? undefined,  // ✅ 添加
  isLiked: false,
  isFavorited: false
};
```

---

### 问题 97：GET /questions/:id 缺少 aiAudit 字段

**优先级**: 高

**问题描述**:
前端 `Question` 类型定义了 `aiAudit` 结构化审核结果，但后端 `GET /questions/:id` 接口返回的数据中没有包含 `aiAudit` 字段。这导致前端无法展示结构化的 AI 审核结果（如是否安全、违规原因、质量建议）。

**后端现状**:
```typescript
// backend/src/services/question.service.ts - getById 方法
return {
  // ... 其他字段
  // ❌ 缺少 aiAudit
};
```

**对比 POST /questions 返回**:
```typescript
// backend/src/services/question.service.ts - create 方法返回
return {
  id: created.id,
  title: created.title,
  // ...
  aiResult: created.aiResult,
  createdAt: created.createdAt,
  // ✅ 返回审核结果供前端显示
  aiAudit: {
    safe: auditResult.safe,
    reason: auditResult.reason,
    qualitySuggestion: auditResult.quality?.suggestion
  }
};
```

**前端现状**:
```typescript
// src/types/index.ts
export interface Question {
  // ...
  aiAudit?: {
    safe: boolean;
    reason?: string;
    qualitySuggestion?: string;
  };
  // ...
}
```

**修复建议**:
```typescript
// 后端 getById 方法补充 aiAudit
let aiAudit = undefined;
if (q.aiResult && q.aiResult !== '无违规') {
  try {
    const aiResult = JSON.parse(q.aiResult);
    aiAudit = {
      safe: aiResult.safe ?? false,
      reason: aiResult.reason,
      qualitySuggestion: aiResult.quality?.suggestion
    };
  } catch {
    aiAudit = undefined;
  }
}

return {
  // ... 其他字段
  aiResult: q.aiResult ?? undefined,
  aiAudit,  // ✅ 添加
  // ...
};
```

---

### 问题 98：后端 createQuestion 返回 authorRole 类型不完整

**优先级**: 中

**问题描述**:
前端 `createQuestion` 调用后期望返回包含 `authorRole` 的问题数据，但后端 `POST /questions` 返回的数据中 `authorRole` 可能为 `undefined`（当查询不到作者信息时）。

**后端现状**:
```typescript
// backend/src/services/question.service.ts - create 方法
const author = await prisma.user.findUnique({
  where: { id: authorId },
  select: { role: true }
});

return {
  // ...
  authorRole: author?.role,  // 可能为 undefined
  // ...
};
```

**前端现状**:
```typescript
// src/services/api.ts
createQuestion: async (payload: CreateQuestionPayload) => {
  const { data } = await api.post<ApiResponse<Question>>('/questions', payload);
  return data.data;
},
```

**问题分析**:
前端 `Question` 类型中 `authorRole?: string` 是可选的，但实际业务中作者角色信息对前端展示有帮助。后端在某些边缘情况下（如开发环境自动创建占位用户失败）可能返回 `undefined`。

**修复建议**:
```typescript
// 后端 create 方法确保 authorRole 有值
const author = await prisma.user.findUnique({
  where: { id: authorId }
});

// 如果找不到作者，使用 fallback
const authorRole = author?.role ?? 'student';

return {
  // ...
  authorRole,  // 确保有值
  // ...
};
```

---

### 问题 99：前端 BackendQuestionListItem 缺少 subject 字段类型

**优先级**: 低

**问题描述**:
前端 `BackendQuestionListItem` 类型中 `subject` 定义为 `subject?: string | null`，但后端 `GET /questions` 返回的数据中 `subject` 始终存在（尽管可能为 `null`）。这导致前端在处理 `subject` 字段时需要进行额外的空值判断。

**后端现状**:
```typescript
// backend/src/services/question.service.ts - list 方法返回
// subject 字段由 Prisma 返回，可能为 null
```

**前端现状**:
```typescript
// src/services/api.ts
type BackendQuestionListItem = {
  id: string;
  title: string;
  // ...
  subject?: string | null;  // 可选且可为 null
};
```

**修复建议**:
```typescript
// 前端统一 BackendQuestionListItem 的 subject 类型
type BackendQuestionListItem = {
  id: string;
  title: string;
  // ...
  subject: string | null | undefined;  // 明确包含 undefined
};
```

---

### 问题 100：前端 getQuestionById 缺少作者信息查询逻辑

**优先级**: 低

**问题描述**:
前端 `questionService.getQuestionById` 直接使用 `ApiResponse<Question>` 类型，但后端返回的作者角色信息可能需要额外的处理。前端在展示问题详情时可能需要知道作者的角色（如教师发布的问题不需要审核）。

**后端现状**:
```typescript
// backend/src/routes/question.routes.ts
const data = await questionService.getById(id, {
  userId: req.user!.id,
  role: req.user!.role
});

return res.json({
  data: {
    ...data,
    // ... 补充用户交互状态
  }
});
```

**前端现状**:
```typescript
// src/services/api.ts
getQuestionById: async (id: string) => {
  const { data } = await api.get<ApiResponse<Question>>(`/questions/${id}`);
  return data.data;
},
```

**修复建议**:
```typescript
// 前端 getQuestionById 添加日志和错误处理
getQuestionById: async (id: string) => {
  const { data } = await api.get<ApiResponse<Question>>(`/questions/${id}`);
  // eslint-disable-next-line no-console
  console.debug('[getQuestionById] response', data.data);
  return data.data;
},
```

---

## 字段映射表

### POST /questions 请求字段

| 字段 | 后端期望 | 前端发送 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| title | ✅ | ✅ | ✅ | 必填，≤100 字符 |
| content | ✅ | ✅ | ✅ | 可选 |
| images | ✅ | ✅ | ✅ | 可选 |
| tags | ✅ | ✅ | ✅ | 可选 |
| difficulty | ✅ | ✅ | ✅ | 可选 |
| subject | ✅ | ✅ | ✅ | 可选 |

### POST /questions 响应字段

| 字段 | 后端返回 | 前端期望 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| id | ✅ | ✅ | ✅ | - |
| title | ✅ | ✅ | ✅ | - |
| content | ✅ | ✅ | ✅ | - |
| images | ✅ | ✅ | ✅ | - |
| tags | ✅ | ✅ | ✅ | - |
| difficulty | ✅ | ✅ | ✅ | - |
| authorId | ✅ | ✅ | ✅ | - |
| authorName | ✅ | ✅ | ✅ | - |
| authorAvatar | ✅ | ✅ | ✅ | 可选 |
| authorRole | ✅ | ✅ | ✅ | 可选 |
| status | ✅ | ✅ | ✅ | - |
| aiResult | ✅ | ✅ | ✅ | - |
| aiAudit | ✅ | ✅ | ✅ | - |
| createdAt | ✅ | ✅ | ✅ | - |

### GET /questions/:id 响应字段

| 字段 | 后端返回 | 前端期望 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| id | ✅ | ✅ | ✅ | - |
| title | ✅ | ✅ | ✅ | - |
| content | ✅ | ✅ | ✅ | - |
| images | ✅ | ✅ | ✅ | - |
| tags | ✅ | ✅ | ✅ | - |
| difficulty | ✅ | ✅ | ✅ | - |
| authorId | ✅ | ✅ | ✅ | - |
| authorName | ✅ | ✅ | ✅ | - |
| authorAvatar | ✅ | ✅ | ✅ | 可选 |
| authorRole | ✅ | ✅ | ✅ | 可选 |
| status | ✅ | ✅ | ✅ | - |
| aiResult | ❌ | ✅ | ❌ | 缺少 |
| aiAudit | ❌ | ✅ | ❌ | 缺少 |
| createdAt | ✅ | ✅ | ✅ | - |
| isLiked | ✅ | ✅ | ✅ | - |
| isFavorited | ✅ | ✅ | ✅ | - |
| understoodCount | ✅ | ✅ | ✅ | 可选 |
| notUnderstoodCount | ✅ | ✅ | ✅ | 可选 |
| likes | ✅ | ✅ | ✅ | - |
| favorites | ✅ | ✅ | ✅ | - |
| comments | ✅ | ✅ | ✅ | - |
| answers | ✅ | ✅ | ✅ | - |

### GET /questions 列表字段

| 字段 | 后端返回 | 前端 BackendQuestionListItem | 是否一致 | 备注 |
|------|----------|-----------------------------|----------|------|
| id | ✅ | ✅ | ✅ | - |
| title | ✅ | ✅ | ✅ | - |
| content | ✅ | ✅ | ✅ | 可选 |
| images | ✅ | ✅ | ✅ | 可选 |
| tags | ✅ | ✅ | ✅ | 可选 |
| difficulty | ✅ | ✅ | ✅ | 可选 |
| subject | ✅ | ⚠️ | ⚠️ | 类型定义为可选 |
| authorId | ✅ | ✅ | ✅ | - |
| authorName | ✅ | ✅ | ✅ | - |
| authorAvatar | ✅ | ✅ | ✅ | 可选 |
| isGoodQuestion | ✅ | ✅ | ✅ | - |
| isPinned | ✅ | ✅ | ✅ | - |
| likes | ✅ | ✅ | ✅ | 可选 |
| favorites | ✅ | ✅ | ✅ | 可选 |
| comments | ✅ | ✅ | ✅ | 可选 |
| answers | ✅ | ✅ | ✅ | 可选 |
| status | ✅ | ✅ | ✅ | - |
| createdAt | ✅ | ✅ | ✅ | - |
| understoodCount | ✅ | ✅ | ✅ | 可选 |
| notUnderstoodCount | ✅ | ✅ | ✅ | 可选 |
| understandingStatus | ✅ | ✅ | ✅ | 可选 |

---

## 优化建议

### 短期优化（高优先级）

1. **GET /questions/:id 补充 aiResult 和 aiAudit**: 确保问题详情接口返回完整的 AI 审核信息
2. **统一创建和详情接口的返回结构**: 避免不同接口返回不一致的数据

### 中期优化（中优先级）

1. **完善 createQuestion 的 authorRole 返回**: 确保作者角色信息始终可用
2. **统一 BackendQuestionListItem 的 subject 类型**: 明确类型定义

### 长期优化（低优先级）

1. **添加问题详情查询的日志**: 便于调试和监控
2. **考虑添加问题浏览量**: 丰富统计信息

---

## 总结

question（问题）模块核心功能完整，但存在以下不一致问题：
1. `GET /questions/:id` 接口缺少 `aiResult` 和 `aiAudit` 字段
2. 创建问题和获取详情接口返回结构不完全一致
3. 列表接口的 `subject` 字段类型定义可优化

建议按优先级逐步修复，确保各接口返回的数据结构一致，提升前端开发体验和数据可预测性。

---

# answer（回答）模块分析

## 模块健康度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 接口完整性 | ⚠️ 中等 | 列表接口缺少 aiResult 和 aiAudit 字段 |
| 类型安全性 | ⚠️ 中等 | 列表返回项缺少审核信息字段 |
| 参数字段一致性 | ✅ 良好 | 参数命名和类型基本一致 |
| 总体健康度 | ⚠️ 中等 | 创建接口返回完整，列表接口返回不完整 |

## 发现的问题

### 问题 101：GET /questions/:id/answers 缺少 aiResult 和 aiAudit 字段

**优先级**: 高

**问题描述**:
前端 `Answer` 类型定义了 `aiResult?: string` 和 `aiAudit?: { safe: boolean; reason?: string }` 字段，但后端 `GET /questions/:id/answers` 列表接口返回的数据中没有包含这两个字段。这导致前端在展示回答列表时无法获取每条回答的 AI 审核状态。

**后端现状**:
```typescript
// backend/src/services/answer.service.ts - list 方法返回项
return {
  id: a.id,
  questionId: a.questionId,
  content: a.content,
  images: a.images,
  audioUrl: a.audioUrl,
  audioUrls: audioUrlsArr,
  authorId: a.authorId,
  authorName: a.authorName,
  authorAvatar: a.authorAvatar ?? undefined,
  likes: a.likes,
  isLiked: likedIds.has(a.id),
  status: a.status,
  createdAt: a.createdAt
  // ❌ 缺少 aiResult 和 aiAudit
};
```

**对比 POST /questions/:id/answers 返回**:
```typescript
// backend/src/services/answer.service.ts - create 方法返回
return {
  // ... 其他字段
  // ✅ 返回审核结果供前端显示
  aiAudit: {
    safe: auditResult.safe,
    reason: auditResult.reason
  }
};
```

**前端现状**:
```typescript
// src/types/index.ts
export interface Answer {
  id: string;
  questionId: string;
  content: string;
  images?: string[];
  audioUrl?: string;
  audioUrls?: string[];
  authorId: string;
  authorName: string;
  authorAvatar?: string;
  likes: number;
  isLiked?: boolean;
  status: AuditStatus;
  aiResult?: string;
  aiAudit?: {
    safe: boolean;
    reason?: string;
  };
  createdAt: string;
}
```

**修复建议**:
```typescript
// 后端 list 方法补充 aiResult 和 aiAudit
return {
  id: a.id,
  questionId: a.questionId,
  content: a.content,
  images: a.images,
  audioUrl: a.audioUrl,
  audioUrls: audioUrlsArr,
  authorId: a.authorId,
  authorName: a.authorName,
  authorAvatar: a.authorAvatar ?? undefined,
  likes: a.likes,
  isLiked: likedIds.has(a.id),
  status: a.status,
  createdAt: a.createdAt,
  aiResult: a.aiResult ?? undefined,  // ✅ 添加
  // aiAudit 需要从 aiResult 解析
  aiAudit: (() => {
    if (a.aiResult && a.aiResult !== '无违规') {
      try {
        const result = JSON.parse(a.aiResult);
        return { safe: result.safe ?? true, reason: result.reason };
      } catch {
        return undefined;
      }
    }
    return undefined;
  })()
};
```

---

### 问题 102：后端 createAnswer 返回缺少 isLiked 字段

**优先级**: 中

**问题描述**:
前端 `Answer` 类型定义了 `isLiked?: boolean` 字段，用于表示当前用户是否已点赞该回答。但后端 `POST /questions/:id/answers` 接口返回的数据中没有包含 `isLiked` 字段。新创建的回答默认没有被点赞，`isLiked` 应为 `false`。

**后端现状**:
```typescript
// backend/src/services/answer.service.ts - create 方法返回
return {
  id: created.id,
  questionId: created.questionId,
  content: created.content,
  images: created.images,
  audioUrl: created.audioUrl,
  audioUrls: audioUrlsArr,
  authorId: created.authorId,
  authorName: created.authorName,
  authorAvatar: created.authorAvatar ?? undefined,
  likes: created.likes,
  status: created.status,
  createdAt: created.createdAt,
  aiAudit: {
    safe: auditResult.safe,
    reason: auditResult.reason
  }
  // ❌ 缺少 isLiked
};
```

**前端现状**:
```typescript
// src/types/index.ts
export interface Answer {
  // ...
  likes: number;
  isLiked?: boolean;  // ✅ 已定义
  // ...
}
```

**修复建议**:
```typescript
// 后端 create 方法补充 isLiked
return {
  id: created.id,
  questionId: created.questionId,
  content: created.content,
  images: created.images,
  audioUrl: created.audioUrl,
  audioUrls: audioUrlsArr,
  authorId: created.authorId,
  authorName: created.authorName,
  authorAvatar: created.authorAvatar ?? undefined,
  likes: created.likes,
  isLiked: false,  // ✅ 新创建的回答未被点赞
  status: created.status,
  createdAt: created.createdAt,
  aiAudit: {
    safe: auditResult.safe,
    reason: auditResult.reason
  }
};
```

---

### 问题 103：前端 answerService.create 缺少专门的有效载荷类型

**优先级**: 低

**问题描述**:
前端 `answerService.create` 方法的 `payload` 参数使用了内联类型 `{ content?: string; images?: string[]; audioUrl?: string; audioUrls?: string[] }`，没有创建专门的类型定义。相比之下，其他服务（如 `questionService`）使用了 `CreateQuestionPayload` 这样的专门类型。

**前端现状**:
```typescript
// src/services/api.ts
export const answerService = {
    create: async (
        questionId: string,
        payload: { content?: string; images?: string[]; audioUrl?: string; audioUrls?: string[] }  // ❌ 内联类型
    ) => {
        const { data } = await api.post<ApiResponse<Answer>>(
            `/questions/${questionId}/answers`,
            payload
        );
        return data.data;
    },
    listByQuestion: async (questionId: string) => {
        const { data } = await api.get<ApiResponse<{ list: Answer[]; total: number }>>(
            `/questions/${questionId}/answers`
        );
        return data.data;
    }
};
```

**修复建议**:
```typescript
// 前端添加 CreateAnswerPayload 类型
export interface CreateAnswerPayload {
    content?: string;
    images?: string[];
    audioUrl?: string;
    audioUrls?: string[];
}

// 使用
export const answerService = {
    create: async (questionId: string, payload: CreateAnswerPayload) => {
        const { data } = await api.post<ApiResponse<Answer>>(
            `/questions/${questionId}/answers`,
            payload
        );
        return data.data;
    },
    // ...
};
```

---

### 问题 104：后端 listByQuestion 缺少作者角色信息返回

**优先级**: 低

**问题描述**:
前端 `Answer` 类型中没有定义 `authorRole` 字段，但后端 `answer.service.ts` 在创建回答时实际上拥有作者的角色信息（从 `author.role` 获取）。列表接口返回的数据中也缺少 `authorRole` 字段，可能影响前端根据作者角色展示不同 UI。

**后端现状**:
```typescript
// backend/src/services/answer.service.ts - list 方法返回
return {
  // ... 其他作者信息
  authorId: a.authorId,
  authorName: a.authorName,
  authorAvatar: a.authorAvatar ?? undefined,
  // ❌ 缺少 authorRole
  // ...
};
```

**前端现状**:
```typescript
// src/types/index.ts
export interface Answer {
  // ...
  authorId: string;
  authorName: string;
  authorAvatar?: string;
  // 没有定义 authorRole
  // ...
}
```

**问题分析**:
虽然前端当前没有定义 `authorRole`，但后端在创建回答时能够获取作者角色信息。如果未来需要展示回答者的身份标识（如教师/学生），建议在前后端同步添加该字段。

**修复建议**:
```typescript
// 前端 Answer 类型补充 authorRole
export interface Answer {
  id: string;
  questionId: string;
  content: string;
  images?: string[];
  audioUrl?: string;
  audioUrls?: string[];
  authorId: string;
  authorName: string;
  authorAvatar?: string;
  authorRole?: string;  // ✅ 添加
  likes: number;
  isLiked?: boolean;
  status: AuditStatus;
  aiResult?: string;
  aiAudit?: {
    safe: boolean;
    reason?: string;
  };
  createdAt: string;
}

// 后端 list 方法补充 authorRole
const author = await prisma.user.findUnique({
  where: { id: a.authorId },
  select: { role: true }
});

return {
  // ...
  authorRole: author?.role,  // ✅ 添加
  // ...
};
```

---

### 问题 105：前端 listByQuestion 返回类型缺少 total 字段使用说明

**优先级**: 低

**问题描述**:
前端 `answerService.listByQuestion` 返回 `{ list: Answer[]; total: number }`，但实际使用中前端组件可能只需要 `list` 字段，`total` 字段在当前页面结构中可能未被使用。建议检查 `total` 字段的实际用途，或在类型定义中明确其用途。

**前端现状**:
```typescript
// src/services/api.ts
listByQuestion: async (questionId: string) => {
    const { data } = await api.get<ApiResponse<{ list: Answer[]; total: number }>>(
        `/questions/${questionId}/answers`
    );
    return data.data;
}
```

**问题分析**:
回答列表目前没有分页，一次性返回所有已审核通过的回答。如果未来需要添加分页功能，`total` 字段将非常有用。建议保留该字段以备将来扩展。

**修复建议**:
```typescript
// 前端添加 AnswerListResponse 类型
export interface AnswerListResponse {
    list: Answer[];
    total: number;  // 保留用于未来分页扩展
}

// 使用
listByQuestion: async (questionId: string) => {
    const { data } = await api.get<ApiResponse<AnswerListResponse>>(
        `/questions/${questionId}/answers`
    );
    return data.data;
}
```

---

## 字段映射表

### POST /questions/:id/answers 请求字段

| 字段 | 后端期望 | 前端发送 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| content | ✅ | ✅ | ✅ | 可选，需配合 images/audioUrls |
| images | ✅ | ✅ | ✅ | 可选 |
| audioUrl | ✅ | ✅ | ✅ | 可选，已废弃，使用 audioUrls |
| audioUrls | ✅ | ✅ | ✅ | 可选，推荐使用 |

### POST /questions/:id/answers 响应字段

| 字段 | 后端返回 | 前端期望 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| id | ✅ | ✅ | ✅ | - |
| questionId | ✅ | ✅ | ✅ | - |
| content | ✅ | ✅ | ✅ | - |
| images | ✅ | ✅ | ✅ | - |
| audioUrl | ✅ | ✅ | ✅ | 可选 |
| audioUrls | ✅ | ✅ | ✅ | - |
| authorId | ✅ | ✅ | ✅ | - |
| authorName | ✅ | ✅ | ✅ | - |
| authorAvatar | ✅ | ✅ | ✅ | 可选 |
| authorRole | ❌ | ❌ | ❌ | 前后端都未返回 |
| likes | ✅ | ✅ | ✅ | - |
| isLiked | ❌ | ✅ | ❌ | 前端期望，后端未返回 |
| status | ✅ | ✅ | ✅ | - |
| aiResult | ✅ | ✅ | ✅ | - |
| aiAudit | ✅ | ✅ | ✅ | - |
| createdAt | ✅ | ✅ | ✅ | - |

### GET /questions/:id/answers 响应字段

| 字段 | 后端返回 | 前端期望 | 是否一致 | 备注 |
|------|----------|----------|----------|------|
| list[].id | ✅ | ✅ | ✅ | - |
| list[].questionId | ✅ | ✅ | ✅ | - |
| list[].content | ✅ | ✅ | ✅ | - |
| list[].images | ✅ | ✅ | ✅ | - |
| list[].audioUrl | ✅ | ✅ | ✅ | 可选 |
| list[].audioUrls | ✅ | ✅ | ✅ | - |
| list[].authorId | ✅ | ✅ | ✅ | - |
| list[].authorName | ✅ | ✅ | ✅ | - |
| list[].authorAvatar | ✅ | ✅ | ✅ | 可选 |
| list[].authorRole | ❌ | ❌ | ❌ | 前后端都未返回 |
| list[].likes | ✅ | ✅ | ✅ | - |
| list[].isLiked | ✅ | ✅ | ✅ | - |
| list[].status | ✅ | ✅ | ✅ | 只返回 approved |
| list[].aiResult | ❌ | ✅ | ❌ | 前端期望，后端未返回 |
| list[].aiAudit | ❌ | ✅ | ❌ | 前端期望，后端未返回 |
| list[].createdAt | ✅ | ✅ | ✅ | - |
| total | ✅ | ✅ | ✅ | 回答总数 |

---

## 优化建议

### 短期优化（高优先级）

1. **GET /questions/:id/answers 补充 aiResult 和 aiAudit**: 确保回答列表接口返回完整的 AI 审核信息
2. **POST /questions/:id/answers 补充 isLiked**: 确保新创建的回答有明确的点赞状态

### 中期优化（中优先级）

1. **创建 CreateAnswerPayload 专门类型**: 统一前端有效载荷类型定义
2. **前后端同步添加 authorRole**: 为未来作者身份展示做准备

### 长期优化（低优先级）

1. **明确 AnswerListResponse 类型定义**: 便于维护和扩展
2. **考虑添加回答分页功能**: 当前一次性返回所有回答可能影响性能

---

## 总结

answer（回答）模块核心功能完整，但存在以下不一致问题：
1. `GET /questions/:id/answers` 列表接口缺少 `aiResult` 和 `aiAudit` 字段
2. `POST /questions/:id/answers` 创建接口缺少 `isLiked` 字段
3. 前后端都缺少 `authorRole` 字段用于展示作者身份
4. 前端缺少 `CreateAnswerPayload` 专门类型

建议按优先级逐步修复，确保各接口返回的数据结构一致，提升前端开发体验和数据可预测性。
