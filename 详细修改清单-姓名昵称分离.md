# 详细修改清单

## 阶段一：数据库迁移

### 1.1 修改 Prisma Schema
**文件：** `backend/prisma/schema.prisma`

**修改位置：** User模型（第10-37行）

**修改内容：**
```prisma
model User {
  id         String   @id @default(cuid())
  phone      String   @unique
  name       String?  // 新增：真实姓名，可选
  nickname   String
  avatar     String?
  // ... 其他字段保持不变
}
```

### 1.2 生成迁移文件
**命令：**
```bash
cd backend
npx prisma migrate dev --name add_name_to_user
```

**输出文件：** `backend/prisma/migrations/YYYYMMDDHHMMSS_add_name_to_user/`

---

## 阶段二：后端代码更新

### 2.1 生成 Prisma Client
**命令：**
```bash
cd backend
npx prisma generate
```

### 2.2 类型定义文件

#### 文件：`backend/src/types/api.ts`（或统一类型文件）

**修改 RegisterPayload 接口：**
```typescript
export interface RegisterPayload extends LoginPayload {
  name?: string;        // 新增：真实姓名
  nickname?: string;
  role?: 'student' | 'teacher' | 'parent';
  grade?: string;
  age?: number;
  school?: string;
  password: string;
}
```

**修改 UpdateProfilePayload（如果存在）：**
```typescript
export interface UpdateProfilePayload {
  name?: string;        // 新增
  nickname?: string;
  avatar?: string;
  grade?: string;
  age?: number;
  school?: string;
}
```

**修改 User 接口（如果在此文件中定义）：**
```typescript
export interface User {
  id: string;
  phone: string;
  name?: string;        // 新增
  nickname: string;
  avatar?: string;
  role: UserRole;
  grade?: string;
  age?: number;
  school?: string;
  expiresAt?: string;
  isValidMember?: boolean;
  permissions?: string[];
}
```

### 2.3 认证路由

**文件：** `backend/src/routes/auth.routes.ts`

**修改位置：** 第72-91行（register路由）

**修改内容：**
```typescript
authRouter.post(
  '/register',
  async (req: Request, res: Response, next: NextFunction) => {
    try {
      const { phone, code, name, nickname, grade, age, school, role, password } = req.body as {
        phone: string;
        code: string;
        name?: string;          // 新增
        nickname?: string;
        grade?: string;
        age?: number;
        school?: string;
        role?: 'student' | 'teacher' | 'parent';
        password?: string;
      };
      const result = await authService.register({
        phone,
        code,
        name,                  // 新增
        nickname,
        grade,
        age,
        school,
        role,
        password
      });
      // ... 其余代码不变
    } catch (err) {
      next(err);
    }
  }
);
```

### 2.4 认证服务

**文件：** `backend/src/services/auth.service.ts`

**修改位置1：** 第482-500行（register方法参数）

```typescript
async register(params: {
  phone: string;
  code: string;
  name?: string;          // 新增
  nickname?: string;
  grade?: string;
  age?: number;
  school?: string;
  role?: 'student' | 'teacher' | 'parent';
  password?: string;
}) {
```

**修改位置2：** 第503-505行（处理nickname逻辑后，添加name处理）

```typescript
// 昵称非必填，若未填写则生成默认昵称
const effectiveNickname = nickname || `用户_${phone.slice(-4)}`;

// 姓名直接使用提供的值（可选）
const effectiveName = name || undefined;
```

**修改位置3：** 第652-666行（创建User数据）

```typescript
user = await prisma.user.create({
  data: {
    phone: normalizedPhone,
    name: effectiveName,                    // 新增
    nickname: effectiveNickname,
    avatar: null,
    role,
    grade: effectiveGrade,
    age: age ?? null,
    school: school ?? null,
    expiresAt,
    passwordHash,
    isActive: true,
    isBanned: false
  }
});
```

**修改位置4：** 第687-700行（内存降级用户对象）

```typescript
user = {
  id: `user_${Date.now()}`,
  phone: normalizedPhone,
  name: effectiveName,                      // 新增
  nickname: effectiveNickname,
  avatar: null,
  role,
  grade: effectiveGrade ?? null,
  age: age ?? null,
  school: school ?? null,
  expiresAt: expiresAt ?? null,
  passwordHash,
  isActive: true,
  isBanned: false,
  createdAt: new Date(),
  updatedAt: new Date(),
  // ... 其他字段
};
```

**修改位置5：** 第310-326行（login返回值）

```typescript
return {
  token,
  refreshToken,
  user: {
    id: user.id,
    phone: user.phone,
    name: user.name ?? undefined,          // 新增
    nickname: user.nickname,
    avatar: user.avatar ?? undefined,
    role: user.role,
    grade: user.grade ?? undefined,
    age: user.age ?? undefined,
    school: user.school ?? undefined,
    expiresAt: undefined,
    isValidMember: undefined,
    permissions: undefined
  }
};
```

**修改位置6：** 第462-478行（passwordLogin返回值）- 同样添加name字段

**修改位置7：** 第743-746行（其他可能返回User的地方，如getMe等）

搜索`nickname: user.nickname`，在每个返回的user对象中添加`name: user.name ?? undefined`

### 2.5 用户路由

**文件：** `backend/src/routes/user-me.routes.ts`

**修改位置1：** 第76行（允许更新的字段）

```typescript
const allowedFields = ['name', 'nickname', 'avatar', 'grade', 'age', 'school']; // 添加 'name'
```

**修改位置2：** 第106-110行（字段检查）

```typescript
if (
  (key === 'name' ||              // 新增
   key === 'nickname' ||
   key === 'avatar' ||
   key === 'grade' ||
   key === 'age' ||
   key === 'school')
) {
```

**修改位置3：** 第143-146行（返回更新后的用户）

```typescript
return {
  id: updatedUser.id,
  phone: updatedUser.phone,
  name: updatedUser.name ?? undefined,    // 新增
  nickname: updatedUser.nickname,
  avatar: updatedUser.avatar,
  role: updatedUser.role,
  grade: updatedUser.grade ?? undefined,
  age: updatedUser.age ?? undefined,
  school: updatedUser.school ?? undefined,
  expiresAt: updatedUser.expiresAt ?? undefined,
  isValidMember: updatedUser.isValidMember ?? undefined,
  permissions: updatedUser.permissions ?? undefined
};
```

### 2.6 用户服务

**文件：** `backend/src/services/user.service.ts`

**修改位置1：** 第9-11行（方法参数）

```typescript
async updateProfile(userId: string, data: {
  name?: string;          // 新增
  nickname?: string;
  avatar?: string;
  grade?: string;
  age?: number;
  school?: string;
}) {
```

**修改位置2：** 第31-38行（数据校验）

```typescript
// 数据校验
if (data.name) {                          // 新增
  if (data.name.length > 20) {
    throw new AppError(400, 'INVALID_PARAMS', '姓名不能超过 20 个字符');
  }
  // 姓名 AI 审核
  const auditResult = await aiAuditService.auditContent(data.name, 'nickname'); // 可能复用nickname审核类型
  if (!auditResult.safe) {
    throw new AppError(400, 'CONTENT_UNSAFE', '姓名包含敏感内容，请修改');
  }
}

if (data.nickname) {
  if (data.nickname.length > 20) {
    throw new AppError(400, 'INVALID_PARAMS', '昵称不能超过 20 个字符');
  }
  // 昵称 AI 审核
  const auditResult = await aiAuditService.auditContent(data.nickname, 'nickname');
  if (!auditResult.safe) {
    throw new AppError(400, 'CONTENT_UNSAFE', '昵称包含敏感内容，请修改');
  }
}
```

**修改位置3：** 第55-58行（更新数据）

```typescript
const updatedUser = await prisma.user.update({
  where: { id: userId },
  data: {
    name: data.name,                      // 新增
    nickname: data.nickname,
    avatar: data.avatar,
    grade: data.grade ?? null,
    age: data.age ?? null,
    school: data.school ?? null
  }
});
```

### 2.7 其他服务（搜索所有返回User的地方）

**需要检查的文件：**
- `backend/src/services/auth.service.ts` - 已完成
- `backend/src/services/user.service.ts` - 已完成
- `backend/src/services/parent.service.ts` - **需要重点修改**
- `backend/src/services/class-hours.service.ts` - 可能返回用户信息
- `backend/src/routes/internal.routes.ts` - 内部路由

#### ParentService修改（重要）

**文件：** `backend/src/services/parent.service.ts`

**修改1：** 第35行，bindChild方法参数类型

```typescript
async bindChild(parentId: string, data: { phone: string; code: string; childName: string; school?: string }) {
```

**修改2：** 第77-82行，更新孩子信息逻辑

**原代码：**
```typescript
// 顺便更新学生信息（如果为空）
if ((!child.nickname || child.nickname.startsWith('用户')) && data.childName) {
  try {
    await prisma.user.update({
      where: { id: child.id },
      data: { nickname: data.childName, school: data.school }
    });
  } catch (e) {
    // ignore update error
  }
}
```

**修改为：**
```typescript
// 顺便更新学生信息（如果为空）
// 孩子姓名应该更新到 name 字段（真实姓名），而非 nickname
if ((!child.nickname || child.nickname.startsWith('用户')) && data.childName) {
  try {
    // 先检查是否需要设置真实姓名
    const needsNameUpdate = !child.name || child.name.trim() === '';
    await prisma.user.update({
      where: { id: child.id },
      data: {
        name: needsNameUpdate ? data.childName : undefined,  // 更新真实姓名（如果为空）
        nickname: (!child.nickname || child.nickname.startsWith('用户')) && data.childName
          ? data.childName  // 如果昵称未设置，用孩子姓名作为默认昵称
          : undefined,
        school: data.school
      }
    });
  } catch (e) {
    // ignore update error
  }
}
```

**修改3：** 第118-122行（getChildren返回值）

```typescript
return relations.map(r => ({
  ...r.child,
  name: r.child.nickname,      // 原有逻辑：name来自nickname（这是显示用的name）
  realName: r.child.name       // 新增：真实姓名（可选），前端可以显示
}));
```

**注意：** 这里的`name`字段是返回给前端用于显示的，实际上是从nickname来的。新增`realName`字段返回真实姓名。或者根据前端实际需求调整。

**修改4：** 前端`src/pages/LoginPage.tsx`对应调整
- 第475行："孩子姓名"标签改为"孩子真实姓名"
- 第478行：placeholder改为"请输入孩子真实姓名"
- 第661行：setBindName状态变量名可以保持不变

### 2.8 AI审核服务（可选）

**文件：** `backend/src/services/ai-audit.service.ts`

检查`auditContent`方法是否支持`'nickname'`类型，如果需要为name单独定义类型，可以添加：

```typescript
export type ContentType = 'question' | 'answer' | 'comment' | 'nickname' | 'name';
```

但通常可以复用nickname的审核规则。

---

## 阶段三：前端代码更新

### 3.1 类型定义

#### 文件：`src/types/index.ts`

**修改 User 接口：**
```typescript
export interface User {
  id: string;
  phone: string;
  name?: string;        // 新增
  nickname: string;
  avatar?: string;
  role: UserRole;

  // 权限相关
  expiresAt?: string;
  isValidMember?: boolean;
  permissions?: string[];

  // 学生专属
  grade?: string;
  age?: number;
  school?: string;
}
```

#### 文件：`src/types/api.ts`

**修改 RegisterPayload：**
```typescript
export interface RegisterPayload extends LoginPayload {
  name?: string;        // 新增
  nickname?: string;
  role?: 'student' | 'teacher' | 'parent';
  grade?: string;
  age?: number;
  school?: string;
  password: string;
}
```

**修改 LoginResponse（如果User类型在此处引用）：**
确保User类型已包含name字段。

### 3.2 注册页面

**文件：** `src/pages/LoginPage.tsx`

**修改1：** 第35-36行，添加name状态
```typescript
// 注册场景下用于展示的姓名（昵称）
const [nickname, setNickname] = useState('');
const [name, setName] = useState('');          // 新增：真实姓名
```

**修改2：** 第363-375行，修改UI（将现有"姓名"改为"昵称"，并新增"真实姓名"）

原代码：
```tsx
{!isLogin && (
  <div className="space-y-2">
    <Label htmlFor="registerName">姓名</Label>
    <Input
      id="registerName"
      type="text"
      placeholder="请输入姓名（用于展示的昵称，可选）"
      value={nickname}
      onChange={(e) => setNickname(e.target.value)}
    />
  </div>
)}
```

修改为：
```tsx
{!isLogin && (
  <>
    {/* 真实姓名 */}
    <div className="space-y-2">
      <Label htmlFor="realName">真实姓名</Label>
      <Input
        id="realName"
        type="text"
        placeholder="请输入真实姓名（可选）"
        value={name}
        onChange={(e) => setName(e.target.value)}
      />
    </div>

    {/* 昵称 */}
    <div className="space-y-2">
      <Label htmlFor="nickname">昵称</Label>
      <Input
        id="nickname"
        type="text"
        placeholder="请输入昵称（用于社区展示，可选）"
        value={nickname}
        onChange={(e) => setNickname(e.target.value)}
      />
    </div>
  </>
)}
```

**修改3：** 第108-247行，handleSubmit函数中的验证逻辑

在注册验证部分（约第135-143行后）添加：
```typescript
// 姓名和昵称验证（如果业务需要）
// 如果两者都为空，可以提示或使用默认值
// 这里选择不强制验证，因为都是可选的
```

**修改4：** 第212-221行，register调用参数

```typescript
const registerResult = await authService.register({
  phone,
  code,
  password,
  name: name.trim() || undefined,           // 新增
  nickname: nickname.trim() || `用户${phone.slice(-4)}`,
  grade: isStudentInvite ? grade : undefined,
  age: isStudentInvite ? parseInt(age, 10) : undefined,
  school: isStudentInvite ? school : undefined,
  role: desiredRole
});
```

**修改5：** 第259行，registerValid验证条件（如果需要）

如果决定name或nickname至少填一个，需要修改验证逻辑：
```typescript
const registerValid =
  !isLogin &&
  !!inviteCode &&
  !!code &&
  password.length >= 8 &&
  (!isStudentInvite || (grade && age && school)) &&
  (name.trim() || nickname.trim());  // 至少填一个
```

但建议保持简单，不添加此验证。

### 3.3 API服务

**文件：** `src/services/api.ts`

**修改位置：** 第408-411行，authService.register的返回类型

实际上代码已经正确：
```typescript
register: async (payload: RegisterPayload) => {
  const { data } = await api.post<ApiResponse<LoginResponse>>('/auth/register', payload);
  return data.data;
},
```

只要RegisterPayload类型已更新，这里会自动推断，无需修改。

### 3.4 家长绑定页面调整

**文件：** `src/pages/LoginPage.tsx`

**修改1：** 第475行，标签修改
```tsx
<Label htmlFor="childName">孩子真实姓名 *</Label>
```

**修改2：** 第478行，placeholder修改
```tsx
placeholder="请输入孩子真实姓名"
```

**说明：** 这个改动是为了与后端逻辑保持一致，让家长填写的是孩子的真实姓名（会更新到User.name），而非昵称。

### 3.5 个人资料页面（可选）

**文件：** `src/pages/ProfilePage.tsx`

**修改1：** 第358行，显示姓名

```tsx
<div className="flex items-center justify-center gap-2 cursor-pointer group"
  onClick={() => {
    setNewNickname(currentUser?.nickname || '');
    setShowNicknameDialog(true);
  }}
>
  <h2 className="text-2xl font-bold text-gray-800 group-hover:text-morandi-5 transition-colors">
    {currentUser?.name || currentUser?.nickname || '未登录'}  {/* 优先显示真实姓名 */}
  </h2>
  <Pencil className="w-4 h-4 text-gray-400 group-hover:text-morandi-5 transition-colors" />
</div>
```

**修改2：** 添加编辑姓名的功能（可选，如果业务需要）

可以复制编辑昵称的逻辑，添加编辑姓名的对话框。但考虑到姓名通常不常修改，且可能涉及实名认证，建议暂不添加，仅显示即可。

### 3.5 其他前端文件

检查是否有其他地方直接使用User.nickname作为"姓名"显示，可能需要调整文案：
- 任何显示"姓名"的地方，如果显示的是nickname，考虑改为"昵称"
- 新增显示真实姓名的位置（如个人资料）

---

## 阶段四：测试更新

### 4.3 家长绑定测试（新增）

**需要测试的场景：**
1. 家长注册并绑定孩子
2. 验证孩子的`name`字段被正确设置为提供的"孩子姓名"
3. 验证孩子的`nickname`字段保持不变（或按逻辑设置）
4. 家长查看孩子列表时，能正确显示孩子的姓名信息

**测试文件：**
- `backend/src/tests/integration/parent.api.spec.ts` - 需要更新
- `test/parent_flow.test.tsx` - 需要更新

---

## 阶段五：数据迁移（生产环境）

### 4.1 后端测试

**需要更新的测试文件（示例）：**

1. `backend/src/tests/unit/auth.service.spec.ts`
   - 第200-220行：注册测试，添加name参数
   - 第260-290行：用户创建断言，添加name字段

2. `backend/src/tests/integration/auth.api.spec.ts`
   - 第148-150行：注册新学生，添加name
   - 第175-177行：注册测试，添加name

3. `backend/src/tests/integration/user-me-profile.api.spec.ts`
   - 第55-64行：更新profile，可以测试name字段

4. 所有包含`nickname: 'xxx'`的测试文件，建议都添加`name: undefined`或`name: 'xxx'`

**批量修改建议：**
使用正则表达式搜索替换：
- 在创建User对象的地方，添加 `name: undefined,` 或 `name: '测试姓名',`

### 4.2 前端测试

**需要更新的测试文件：**

1. `test/integration.profile-nickname.test.tsx` - 可能需要重命名或更新
2. `test/user_scenarios.test.tsx` - 注册场景测试
3. `test/full_p0_coverage.test.tsx` - 覆盖注册流程

**主要修改：**
- 注册表单测试，验证新增的姓名字段
- 提交后的用户数据验证

---

## 阶段五：数据迁移（生产环境）

### 5.1 方案A：无迁移（推荐）
- 不执行任何数据迁移
- 现有用户的name字段为NULL
- 用户后续可以自愿填写

**优点：** 零风险，零停机
**缺点：** 历史用户没有真实姓名

### 5.2 方案B：一次性数据迁移脚本

**创建脚本：** `backend/script/migrate-user-name.ts`

```typescript
import { prisma } from '../src/config/database';

async function migrate() {
  // 将所有现有用户的nickname复制到name字段（如果name为空）
  const result = await prisma.user.updateMany({
    where: {
      name: null
    },
    data: {
      name: prisma.user.nickname
    }
  };

  console.log(`Updated ${result.count} users`);
}

migrate()
  .then(() => {
    console.log('Migration completed');
    process.exit(0);
  })
  .catch(err => {
    console.error('Migration failed:', err);
    process.exit(1);
  });
```

**执行：**
```bash
cd backend
npx tsx script/migrate-user-name.ts
```

**风险：** 可能将昵称误作为真实姓名，需业务确认。

---

## 检查清单

### 数据库
- [ ] 修改 schema.prisma，添加 name 字段
- [ ] 生成迁移文件
- [ ] 本地测试迁移
- [ ] 提交迁移文件到 Git

### 后端
- [ ] 生成 Prisma Client
- [ ] 更新所有类型定义文件
- [ ] 修改 auth.routes.ts
- [ ] 修改 auth.service.ts（register、login、passwordLogin）
- [ ] 修改 user-me.routes.ts
- [ ] 修改 user.service.ts
- [ ] 检查其他服务（parent、class-hours等）
- [ ] 更新所有测试文件（约20+个）
- [ ] 运行所有测试通过

### 前端
- [ ] 更新 types/index.ts
- [ ] 更新 types/api.ts
- [ ] 修改 LoginPage.tsx（状态、UI、验证、提交）
- [ ] 修改 LoginPage.tsx（家长绑定：childName标签和placeholder）
- [ ] 修改 ProfilePage.tsx（显示姓名）
- [ ] 更新相关测试
- [ ] 本地测试注册流程
- [ ] 本地测试家长绑定流程

### 部署
- [ ] 备份生产数据库
- [ ] 执行数据库迁移
- [ ] 部署后端代码
- [ ] 部署前端代码
- [ ] 验证注册功能
- [ ] 监控日志和错误

---

## 注意事项

1. **字段可为空**：name字段必须是`String?`（可选），避免破坏现有数据
2. **向后兼容**：注册接口的name参数应为可选，旧版本客户端仍可使用
3. **AI审核**：name字段需要经过AI审核，与nickname相同的审核规则
4. **隐私保护**：name是敏感信息，确保只在必要接口返回
5. **测试充分**：特别是注册流程和用户更新流程
6. **文档更新**：如果有的API文档，需要同步更新

---

## 回滚方案

如果出现问题，按以下步骤回滚：

1. **数据库回滚**：
   ```bash
   cd backend
   npx prisma migrate resolve --rolled-back <migration_name>
   ```

2. **代码回滚**：
   - Git revert所有相关提交
   - 重新部署旧版本代码

3. **数据恢复**：
   - 如果已执行数据迁移，需要从备份恢复
   - 或手动删除name字段数据

---

## 后续优化（可选）

1. **白名单预填**：注册时根据手机号从白名单获取name预填
2. **姓名编辑**：在个人资料页面添加姓名编辑功能
3. **实名认证**：结合姓名和身份证号进行实名认证
4. **统计分析**：统计用户填写真实姓名的比例
5. **权限控制**：某些功能需要真实姓名才能使用
