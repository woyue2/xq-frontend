# 知识星球问答小程序 - 后端需求文档

## 一、系统概述

### 1.1 应用架构
- **前端**: React + TypeScript
- **后端需求**: RESTful API 或 GraphQL
- **数据库**: PostgreSQL
- **存储**: 图片使用图床，音频存放本地，视频存放外部链接
- **AI服务**: 内容审核API接入

### 1.2 用户角色权限
| 角色 | 权限说明 |
|-----|---------|
| **学生** | 可提问、点赞、收藏、评论（自己的问题） |
| **家长** | 可点赞、收藏 |
| **教师** | 可提问、回答、录音、点赞、收藏、评论、审核、置顶、打分 |

---

## 二、数据库设计

### 2.1 核心数据表

#### **用户表 (users)**
```sql
id              VARCHAR(36) PRIMARY KEY
phone           VARCHAR(11) UNIQUE NOT NULL
nickname        VARCHAR(50) NOT NULL
avatar          VARCHAR(500)
role            ENUM('student', 'parent', 'teacher') NOT NULL
created_at      TIMESTAMP DEFAULT NOW()
updated_at      TIMESTAMP DEFAULT NOW()
is_active       BOOLEAN DEFAULT TRUE
is_banned       BOOLEAN DEFAULT FALSE
```

#### **问题表 (questions)**
```sql
id              VARCHAR(36) PRIMARY KEY
title           VARCHAR(100) NOT NULL
content         TEXT
author_id       VARCHAR(36) NOT NULL (FK users.id)
status          ENUM('pending', 'approved', 'rejected', 'banned') DEFAULT 'pending'
ai_result       TEXT          -- AI审核结果
score           INT           -- 1-5分评分
is_good_question BOOLEAN DEFAULT FALSE
is_pinned       BOOLEAN DEFAULT FALSE
pinned_at       TIMESTAMP     -- 置顶时间
reject_reason   TEXT          -- 驳回原因
likes_count     INT DEFAULT 0
favorites_count INT DEFAULT 0
comments_count  INT DEFAULT 0
answers_count   INT DEFAULT 0
created_at      TIMESTAMP DEFAULT NOW()
updated_at      TIMESTAMP DEFAULT NOW()
deleted_at      TIMESTAMP     -- 软删除
```

#### **问题图片表 (question_images)**
```sql
id              VARCHAR(36) PRIMARY KEY
question_id     VARCHAR(36) NOT NULL (FK questions.id)
image_url       VARCHAR(500) NOT NULL
sort_order      INT DEFAULT 0
created_at      TIMESTAMP DEFAULT NOW()
```

#### **问题音频表 (question_audios)**
```sql
id              VARCHAR(36) PRIMARY KEY
question_id     VARCHAR(36) NOT NULL (FK questions.id)
audio_url       VARCHAR(500) NOT NULL
duration        INT           -- 秒
file_size       INT           -- 字节
created_at      TIMESTAMP DEFAULT NOW()
```

#### **问题标签表 (question_tags)**
```sql
id              VARCHAR(36) PRIMARY KEY
question_id     VARCHAR(36) NOT NULL (FK questions.id)
tag_name        VARCHAR(50) NOT NULL    -- 如"二次函数"
created_at      TIMESTAMP DEFAULT NOW()
```

#### **问题难度表 (question_difficulties)**
```sql
id              VARCHAR(36) PRIMARY KEY
question_id     VARCHAR(36) NOT NULL (FK questions.id)
difficulty      ENUM('easy', 'medium', 'hard') NOT NULL
created_at      TIMESTAMP DEFAULT NOW()
```

#### **回答表 (answers)**
```sql
id              VARCHAR(36) PRIMARY KEY
question_id     VARCHAR(36) NOT NULL (FK questions.id)
content         TEXT NOT NULL
author_id       VARCHAR(36) NOT NULL (FK users.id)
status          ENUM('pending', 'approved', 'rejected', 'banned') DEFAULT 'pending'
ai_result       TEXT
likes_count     INT DEFAULT 0
created_at      TIMESTAMP DEFAULT NOW()
updated_at      TIMESTAMP DEFAULT NOW()
deleted_at      TIMESTAMP
```

#### **回答图片表 (answer_images)**
```sql
id              VARCHAR(36) PRIMARY KEY
answer_id       VARCHAR(36) NOT NULL (FK answers.id)
image_url       VARCHAR(500) NOT NULL
sort_order      INT DEFAULT 0
created_at      TIMESTAMP DEFAULT NOW()
```

#### **回答音频表 (answer_audios)**
```sql
id              VARCHAR(36) PRIMARY KEY
answer_id       VARCHAR(36) NOT NULL (FK answers.id)
audio_url       VARCHAR(500) NOT NULL
duration        INT
file_size       INT
created_at      TIMESTAMP DEFAULT NOW()
```

#### **评论表 (comments)**
```sql
id              VARCHAR(36) PRIMARY KEY
question_id     VARCHAR(36) NOT NULL (FK questions.id)
content         TEXT NOT NULL
image_url       VARCHAR(500)
author_id       VARCHAR(36) NOT NULL (FK users.id)
status          ENUM('pending', 'approved', 'rejected', 'banned') DEFAULT 'pending'
ai_result       TEXT
created_at      TIMESTAMP DEFAULT NOW()
updated_at      TIMESTAMP DEFAULT NOW()
deleted_at      TIMESTAMP
```

#### **点赞表 (likes)**
```sql
id              VARCHAR(36) PRIMARY KEY
user_id         VARCHAR(36) NOT NULL (FK users.id)
target_type     ENUM('question', 'answer') NOT NULL
target_id       VARCHAR(36) NOT NULL
created_at      TIMESTAMP DEFAULT NOW()
UNIQUE KEY (user_id, target_type, target_id)
```

#### **收藏表 (favorites)**
```sql
id              VARCHAR(36) PRIMARY KEY
user_id         VARCHAR(36) NOT NULL (FK users.id)
question_id     VARCHAR(36) NOT NULL (FK questions.id)
created_at      TIMESTAMP DEFAULT NOW()
UNIQUE KEY (user_id, question_id)
```

#### **邀请码表 (invite_codes)**
```sql
id              VARCHAR(36) PRIMARY KEY
code            VARCHAR(50) UNIQUE NOT NULL
role            ENUM('student', 'parent', 'teacher') NOT NULL
max_uses        INT DEFAULT 0          -- 0表示无限制
used_count      INT DEFAULT 0
is_active       BOOLEAN DEFAULT TRUE
created_at      TIMESTAMP DEFAULT NOW()
expires_at      TIMESTAMP
```

#### **审核日志表 (audit_logs)**
```sql
id              VARCHAR(36) PRIMARY KEY
auditor_id      VARCHAR(36) NOT NULL (FK users.id)
target_type     ENUM('question', 'answer', 'comment') NOT NULL
target_id       VARCHAR(36) NOT NULL
action          ENUM('approve', 'reject', 'ban') NOT NULL
reason          TEXT
created_at      TIMESTAMP DEFAULT NOW()
```

---

## 三、API接口设计

### 3.1 认证相关 (Auth)

#### **1. 发送验证码**
```
POST /api/auth/send-code
请求体: { phone: string }
响应: { success: boolean, message: string }
```

#### **2. 登录**
```
POST /api/auth/login
请求体: { phone: string, code: string }
响应: { 
  token: string, 
  user: { 
    id, nickname, avatar, role, phone 
  } 
}
```

#### **3. 注册**
```
POST /api/auth/register
请求体: { 
  phone: string, 
  code: string, 
  inviteCode: string,
  nickname?: string 
}
响应: { 
  token: string, 
  user: { id, nickname, avatar, role, phone } 
}
```

#### **4. 验证邀请码**
```
POST /api/auth/validate-invite-code
请求体: { code: string }
响应: { valid: boolean, role: string }
```

#### **5. 退出登录**
```
POST /api/auth/logout
请求头: Authorization: Bearer {token}
响应: { success: boolean }
```

---

### 3.2 用户相关 (User)

#### **6. 获取当前用户信息**
```
GET /api/user/profile
请求头: Authorization: Bearer {token}
响应: { 
  id, nickname, avatar, role, phone,
  stats: {
    questionsCount, answersCount, likesCount, favoritesCount
  }
}
```

#### **7. 更新用户信息**
```
PUT /api/user/profile
请求头: Authorization: Bearer {token}
请求体: { nickname?: string, avatar?: string }
响应: { success: boolean, user: {...} }
```

#### **8. 上传头像**
```
POST /api/user/avatar
请求头: Authorization: Bearer {token}
请求体: FormData { file: File }
响应: { avatarUrl: string }
```

---

### 3.3 问题相关 (Question)

#### **9. 获取问题列表（首页）**
```
GET /api/questions?page=1&limit=20&sort=latest&filter=all
请求头: Authorization: Bearer {token}
查询参数:
  - page: 页码
  - limit: 每页数量
  - sort: latest(最新) | hot(热门) | pinned(置顶优先)
  - filter: all | good(好问题) | tag(标签筛选)
响应: {
  questions: [
    {
      id, title, content, images, audioUrl,
      author: { id, nickname, avatar },
      tags, difficulty, isGoodQuestion, isPinned,
      likes, favorites, comments, answers,
      status, score, createdAt
    }
  ],
  total, page, totalPages
}
```

#### **10. 获取问题详情**
```
GET /api/questions/:id
请求头: Authorization: Bearer {token}
响应: {
  question: { ...完整问题信息 },
  answers: [ ...回答列表 ],
  comments: [ ...评论列表 ],
  userActions: {
    liked: boolean,
    favorited: boolean
  }
}
```

#### **11. 创建问题**
```
POST /api/questions
请求头: Authorization: Bearer {token}
请求体: {
  title: string (必填, max 100),
  content?: string (max 500),
  images?: string[] (max 3),
  audioUrl?: string,
  tags?: string[],
  difficulty?: 'easy' | 'medium' | 'hard'
}
响应: { 
  success: boolean, 
  question: { id, status, ... } 
}
权限: 学生、教师
```

#### **12. 上传问题图片**
```
POST /api/questions/upload-image
请求头: Authorization: Bearer {token}
请求体: FormData { file: File }
响应: { imageUrl: string }
权限: 学生、教师
```

#### **13. 上传问题音频**
```
POST /api/questions/upload-audio
请求头: Authorization: Bearer {token}
请求体: FormData { file: File }
响应: { audioUrl: string, duration: number }
权限: 教师（学生无权限）
```

#### **14. 置顶/取消置顶问题**
```
PATCH /api/questions/:id/pin
请求头: Authorization: Bearer {token}
请求体: { isPinned: boolean }
响应: { success: boolean }
权限: 教师
```

#### **15. 删除问题（软删除）**
```
DELETE /api/questions/:id
请求头: Authorization: Bearer {token}
响应: { success: boolean }
权限: 问题作者或管理员
```

---

### 3.4 回答相关 (Answer)

#### **16. 创建回答**
```
POST /api/answers
请求头: Authorization: Bearer {token}
请求体: {
  questionId: string,
  content: string (max 2000),
  images?: string[] (max 5),
  audioUrl?: string
}
响应: { 
  success: boolean, 
  answer: { id, status, ... } 
}
权限: 教师
```

#### **17. 上传回答图片**
```
POST /api/answers/upload-image
请求头: Authorization: Bearer {token}
请求体: FormData { file: File }
响应: { imageUrl: string }
权限: 教师
```

#### **18. 上传回答音频**
```
POST /api/answers/upload-audio
请求头: Authorization: Bearer {token}
请求体: FormData { file: File }
响应: { audioUrl: string, duration: number }
权限: 教师
```

#### **19. 删除回答（软删除）**
```
DELETE /api/answers/:id
请求头: Authorization: Bearer {token}
响应: { success: boolean }
权限: 回答作者或管理员
```

---

### 3.5 评论相关 (Comment)

#### **20. 获取评论列表**
```
GET /api/comments?questionId={questionId}&page=1&limit=50
请求头: Authorization: Bearer {token}
响应: {
  comments: [
    {
      id, questionId, content, image,
      author: { id, nickname, avatar },
      status, createdAt
    }
  ],
  total
}
```

#### **21. 创建评论**
```
POST /api/comments
请求头: Authorization: Bearer {token}
请求体: {
  questionId: string,
  content: string,
  image?: string
}
响应: { 
  success: boolean, 
  comment: { id, status, ... } 
}
权限: 提问者、教师
```

#### **22. 上传评论图片**
```
POST /api/comments/upload-image
请求头: Authorization: Bearer {token}
请求体: FormData { file: File }
响应: { imageUrl: string }
```

#### **23. 删除评论（软删除）**
```
DELETE /api/comments/:id
请求头: Authorization: Bearer {token}
响应: { success: boolean }
权限: 评论作者或管理员
```

---

### 3.6 互动相关 (Interaction)

#### **24. 点赞/取消点赞**
```
POST /api/interactions/like
请求头: Authorization: Bearer {token}
请求体: {
  targetType: 'question' | 'answer',
  targetId: string,
  action: 'like' | 'unlike'
}
响应: { success: boolean, liked: boolean, likesCount: number }
权限: 所有用户
```

#### **25. 收藏/取消收藏**
```
POST /api/interactions/favorite
请求头: Authorization: Bearer {token}
请求体: {
  questionId: string,
  action: 'favorite' | 'unfavorite'
}
响应: { success: boolean, favorited: boolean, favoritesCount: number }
权限: 所有用户
```

#### **26. 获取我的点赞列表**
```
GET /api/interactions/my-likes?page=1&limit=20
请求头: Authorization: Bearer {token}
响应: {
  likes: [ { targetType, target: {...}, createdAt } ],
  total
}
```

#### **27. 获取我的收藏列表**
```
GET /api/interactions/my-favorites?page=1&limit=20
请求头: Authorization: Bearer {token}
响应: {
  favorites: [ { question: {...}, createdAt } ],
  total
}
```

---

### 3.7 审核相关 (Audit)

#### **28. 获取待审核内容列表**
```
GET /api/audit/pending?type=question&page=1&limit=20
请求头: Authorization: Bearer {token}
查询参数:
  - type: question | answer | comment
  - status: pending | approved | rejected | banned
响应: {
  items: [
    {
      id, title/content, author, images, audioUrl,
      aiResult, status, createdAt
    }
  ],
  counts: {
    pending, approved, rejected, banned
  },
  total
}
权限: 教师
```

#### **29. 审核通过**
```
POST /api/audit/approve
请求头: Authorization: Bearer {token}
请求体: {
  type: 'question' | 'answer' | 'comment',
  id: string
}
响应: { success: boolean }
权限: 教师
```

#### **30. 审核驳回**
```
POST /api/audit/reject
请求头: Authorization: Bearer {token}
请求体: {
  type: 'question' | 'answer' | 'comment',
  id: string,
  reason: string
}
响应: { success: boolean }
权限: 教师
```

#### **31. 封禁内容**
```
POST /api/audit/ban
请求头: Authorization: Bearer {token}
请求体: {
  type: 'question' | 'answer' | 'comment',
  id: string,
  reason: string
}
响应: { success: boolean }
权限: 教师
```

#### **32. 标记好问题**
```
POST /api/audit/mark-good-question
请求头: Authorization: Bearer {token}
请求体: {
  questionId: string,
  isGoodQuestion: boolean
}
响应: { success: boolean }
权限: 教师
```

#### **33. 问题评分**
```
POST /api/audit/score-question
请求头: Authorization: Bearer {token}
请求体: {
  questionId: string,
  score: number (1-5)
}
响应: { success: boolean }
权限: 教师
```

#### **34. 获取审核统计**
```
GET /api/audit/stats
请求头: Authorization: Bearer {token}
响应: {
  questions: { pending, approved, rejected, banned },
  answers: { pending, approved, rejected, banned },
  comments: { pending, approved, rejected, banned }
}
权限: 教师
```

---

### 3.8 个人中心相关 (Profile)

#### **35. 获取我的提问列表**
```
GET /api/profile/my-questions?page=1&limit=20&status=all
请求头: Authorization: Bearer {token}
响应: {
  questions: [ {...} ],
  total
}
```

#### **36. 获取我的回答列表**
```
GET /api/profile/my-answers?page=1&limit=20
请求头: Authorization: Bearer {token}
响应: {
  answers: [ {...} ],
  total
}
权限: 教师
```

#### **37. 获取个人统计数据**
```
GET /api/profile/stats
请求头: Authorization: Bearer {token}
响应: {
  questionsCount,
  answersCount,
  likesReceived,
  favoritesReceived,
  commentsReceived
}
```

---

## 四、功能需求细分

### 4.1 学生端功能

#### **A. 登录/注册页面**
1. ✅ 发送手机验证码
2. ✅ 验证手机号合法性
3. ✅ 验证邀请码有效性
4. ✅ 创建学生账号
5. ✅ 登录并获取 JWT Token
6. ⚠️ 验证码防刷机制（60秒倒计时）
7. ⚠️ 登录日志记录

#### **B. 问答首页**
1. ✅ 获取问题列表（分页、排序）
2. ✅ 置顶问题优先展示
3. ✅ 显示问题标签、难度、好问题标记
4. ✅ 点赞/取消点赞问题
5. ✅ 收藏/取消收藏问题
6. ✅ 查看问题详情（跳转）
7. ✅ 实时更新点赞/收藏数
8. ⚠️ 搜索功能（标题、标签）
9. ⚠️ 标签筛选

#### **C. 问题编辑/提问页**
1. ✅ 输入问题标题（100字限制）
2. ✅ 输入问题详情（500字限制）
3. ✅ 上传图片（最多3张）
4. ✅ 删除已上传图片
5. ✅ 提交问题到后端
6. ✅ AI初筛（后端实现）
7. ✅ 问题进入待审核状态
8. ⚠️ 自动保存草稿
9. ⚠️ 退出时提示保存

#### **D. 问题详情页**
1. ✅ 显示问题完整信息
2. ✅ 显示问题图片（点击放大）
3. ✅ 播放问题音频（如果有）
4. ✅ 显示审核通过的回答列表
5. ✅ 显示审核通过的评论列表
6. ✅ 点赞/收藏问题
7. ✅ 评论自己提的问题
8. ✅ 上传评论图片
9. ✅ 分享问题链接
10. ⚠️ 评论分页加载

#### **E. 个人中心页**
1. ✅ 显示用户信息（昵称、头像、角色）
2. ✅ 修改昵称
3. ✅ 修改头像（预设头像选择）
4. ✅ 查看我的点赞
5. ✅ 查看我的收藏
6. ✅ 查看我的提问
7. ✅ 退出登录
8. ⚠️ 个人统计数据（提问数、获赞数等）

---

### 4.2 教师端功能

#### **A. 登录/注册页面（同学生端）**
1. ✅ 使用教师邀请码注册
2. ✅ 账号角色标记为 teacher

#### **B. 问答首页（增强权限）**
1. ✅ 查看所有功能（同学生端）
2. ✅ 显示"去回答"按钮
3. ✅ 置顶/取消置顶问题按钮
4. ✅ 可以提问（与学生相同）

#### **C. 问题编辑/提问页（同学生端）**
1. ✅ 教师可以提问
2. ✅ 提交问题流程与学生相同

#### **D. 回答问题页（教师专属）**
1. ✅ 输入回答内容（2000字限制）
2. ✅ 上传回答图片（最多5张）
3. ✅ 录制语音回答（教师专属权限）
4. ✅ 播放录音预览
5. ✅ 删除录音重新录制
6. ✅ 提交回答到后端
7. ✅ AI初筛（后端实现）
8. ✅ 回答进入待审核状态

#### **E. 问题详情页（增强权限）**
1. ✅ 显示"去回答"按钮
2. ✅ 可以评论任何问题
3. ✅ 播放自己的语音回答

#### **F. 审核管理页（教师专属）**
1. ✅ 查看待审核问题列表
2. ✅ 查看待审核评论列表
3. ✅ 查看问题的AI审核结果
4. ✅ 审核通过问题/评论
5. ✅ 审核驳回（填写原因）
6. ✅ 封禁违规内容
7. ✅ 标记"好问题"
8. ✅ 给问题打分（1-5分）
9. ✅ 查看问题图片（点击放大）
10. ✅ 筛选审核状态（待审核/已通过/已驳回）
11. ⚠️ 批量审核操作

#### **G. 个人中心页（增强功能）**
1. ✅ 显示教师身份标识
2. ✅ 查看我的提问
3. ✅ 查看我的回答
4. ✅ 进入审核管理页入口
5. ⚠️ 教师统计数据（回答数、审核数等）

---

## 五、核心业务逻辑

### 5.1 审核流程

#### **AI初筛 + 人工审核双重机制**

```
1. 用户提交内容（问题/回答/评论）
   ↓
2. 后端调用AI内容审核API
   - 检测敏感词、违规内容
   - 生成审核结果（aiResult字段）
   ↓
3. 内容状态设为 'pending'（待审核）
   ↓
4. 教师在审核管理页查看
   ↓
5. 教师决策：
   - 审核通过 → status = 'approved'
   - 审核驳回 → status = 'rejected' + 填写原因
   - 封禁内容 → status = 'banned'
   ↓
6. 前端仅显示 status = 'approved' 的内容
```

#### **审核权限**
- 仅教师可执行审核操作
- 审核日志记录到 `audit_logs` 表

---

### 5.2 录音权限控制

| 功能 | 学生 | 教师 |
|-----|------|------|
| 问题录音 | ❌ | ❌ |
| 回答录音 | ❌ | ✅ |

**实现方式：**
- 前端根据 `currentUser.role` 动态显示录音功能
- 后端上传接口验证用户角色
- 如果学生尝试上传音频，返回 403 Forbidden

---

### 5.3 评论权限控制

| 场景 | 权限规则 |
|-----|---------|
| 提问者评论自己的问题 | ✅ 允许 |
| 教师评论任何问题 | ✅ 允许 |
| 其他用户评论 | ❌ 禁止 |

**实现方式：**
```javascript
// 后端 API 伪代码
if (comment.userId !== question.authorId && user.role !== 'teacher') {
  return res.status(403).json({ error: '无评论权限' });
}
```

---

### 5.4 置顶功能

#### **规则：**
- 仅教师可置顶/取消置顶
- 置顶问题优先显示在列表顶部
- 记录置顶时间 `pinned_at`

#### **排序逻辑：**
```sql
ORDER BY 
  is_pinned DESC,        -- 置顶优先
  pinned_at DESC,        -- 置顶时间倒序
  created_at DESC        -- 创建时间倒序
```

---

### 5.5 好问题标记

#### **规则：**
- 仅教师可标记/取消标记
- 标记后显示红色"好问题"徽章
- 可配合评分功能使用（如评分>=4自动标记）

---

### 5.6 点赞/收藏防重

#### **方案1：数据库唯一索引**
```sql
-- likes表
UNIQUE KEY (user_id, target_type, target_id)

-- favorites表
UNIQUE KEY (user_id, question_id)
```

#### **方案2：前端状态管理**
- 前端维护 `likedQuestions` 和 `favoritedQuestions` Set
- 用户操作后立即更新UI
- 调用API成功后同步到后端

---

## 六、安全性要求

### 6.1 认证与授权
1. ✅ JWT Token 认证
2. ✅ Token过期时间设置（7天）
3. ✅ 角色权限验证中间件
4. ⚠️ 刷新Token机制
5. ⚠️ IP限流（防止接口滥用）

### 6.2 数据验证
1. ✅ 手机号格式验证
2. ✅ 邀请码有效性验证
3. ✅ 字段长度限制（标题100字、内容500/2000字）
4. ✅ 文件类型验证（图片、音频）
5. ✅ 文件大小限制（图片5MB、音频10MB）
6. ⚠️ SQL注入防护
7. ⚠️ XSS防护

### 6.3 内容安全
1. ✅ AI内容审核（接入阿里云、腾讯云等）
2. ✅ 敏感词过滤
3. ✅ 图片鉴黄
4. ✅ 音频违规检测
5. ⚠️ 用户举报机制

---

## 七、性能优化

### 7.1 数据库优化
1. ⚠️ 添加索引（user_id, question_id, status, created_at）
2. ⚠️ 分库分表（海量数据场景）
3. ⚠️ 读写分离

### 7.2 缓存策略
1. ⚠️ Redis缓存热门问题
2. ⚠️ 缓存用户点赞/收藏状态
3. ⚠️ 缓存问题列表（5分钟过期）
4. ⚠️ CDN加速图片/音频

### 7.3 接口优化
1. ⚠️ 分页加载（首页20条/页）
2. ⚠️ 图片懒加载
3. ⚠️ 回答/评论异步加载
4. ⚠️ GraphQL按需查询字段

---

## 八、第三方服务

### 8.1 短信服务
- **阿里云短信** 或 **腾讯云短信**
- 用途：发送登录验证码

### 8.2 对象存储
- **阿里云OSS** 或 **腾讯云COS** 或 **七牛云**
- 用途：存储图片、音频文件
- 返回CDN加速地址

### 8.3 内容审核
- **阿里云内容安全** 或 **腾讯云天御**
- 审核项：文本、图片、音频
- 返回审核结果（通过/可疑/违规）

### 8.4 音频处理
- **阿里云语音识别** 或 **科大讯飞**
- 用途：音频转文字（可选）
- 用途：音频压缩（减少存储成本）

---

## 九、部署与运维

### 9.1 服务器配置
- **应用服务器**: 2核4G起步（支持并发500+）
- **数据库服务器**: 4核8G（MySQL/PostgreSQL）
- **Redis服务器**: 2核4G

### 9.2 监控与日志
1. ⚠️ API请求日志
2. ⚠️ 错误日志（Sentry/ELK）
3. ⚠️ 性能监控（响应时间、QPS）
4. ⚠️ 数据库慢查询监控

### 9.3 备份策略
1. ⚠️ 数据库每日全量备份
2. ⚠️ 文件存储7天保留策略
3. ⚠️ 灾难恢复预案

---

## 十、开发优先级

### Phase 1 - 核心功能（MVP）
✅ 表示前端已完成，需后端实现

1. ✅ 用户登录/注册（API 1-5）
2. ✅ 问题CRUD（API 9-15）
3. ✅ 回答CRUD（API 16-19）
4. ✅ 评论CRUD（API 20-23）
5. ✅ 点赞/收藏（API 24-27）
6. ✅ 文件上传（图片、音频）

### Phase 2 - 审核系统
1. ✅ AI内容审核接入
2. ✅ 教师审核管理（API 28-34）
3. ✅ 好问题标记
4. ✅ 问题评分
5. ✅ 置顶功能

### Phase 3 - 优化与扩展
1. ⚠️ 搜索功能
2. ⚠️ 标签系统
3. ⚠️ 个人中心完整统计
4. ⚠️ 消息通知（审核结果、回答提醒）
5. ⚠️ 用户举报机制

### Phase 4 - 性能与安全
1. ⚠️ Redis缓存
2. ⚠️ CDN加速
3. ⚠️ 接口限流
4. ⚠️ 日志监控
5. ⚠️ 数据备份

---

## 十一、测试用例

### 11.1 单元测试
- 用户注册验证逻辑
- 权限验证中间件
- 文件上传验证
- 内容审核逻辑

### 11.2 集成测试
- 完整提问流程
- 完整回答流程
- 审核流程
- 点赞/收藏流程

### 11.3 压力测试
- 并发用户登录
- 高频点赞请求
- 文件上传性能
- 数据库查询性能

---

## 十二、后端技术栈推荐

### 方案1：Node.js + Express
- **优势**: 与前端同语言，开发效率高
- **推荐框架**: Express.js / Nest.js
- **ORM**: Prisma / TypeORM
- **数据库**: PostgreSQL / MySQL

### 方案2：Python + Django/Flask
- **优势**: AI集成方便，生态丰富
- **推荐框架**: Django REST Framework / FastAPI
- **ORM**: Django ORM / SQLAlchemy
- **数据库**: PostgreSQL

### 方案3：Java + Spring Boot
- **优势**: 企业级稳定性，性能优秀
- **推荐框架**: Spring Boot + MyBatis
- **数据库**: MySQL
- **缓存**: Redis

---

## 十三、前后端协作流程

1. **接口文档**: 使用 Swagger/Postman 生成API文档
2. **Mock数据**: 前端使用 Mock.js 模拟接口（已实现）
3. **联调测试**: 后端完成后替换 Mock 数据
4. **错误处理**: 统一错误码规范
5. **版本管理**: Git分支策略（dev/staging/production）

---

## 附录：错误码规范

| 错误码 | 说明 |
|-------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 429 | 请求过于频繁 |
| 500 | 服务器错误 |
| 1001 | 手机号格式错误 |
| 1002 | 验证码错误 |
| 1003 | 邀请码无效 |
| 2001 | 文件类型不支持 |
| 2002 | 文件过大 |
| 3001 | 无提问权限 |
| 3002 | 无回答权限 |
| 3003 | 无评论权限 |
| 3004 | 无审核权限 |

---

**文档版本**: v1.0  
**最后更新**: 2026-01-31  
**维护人员**: AI Assistant  

---

## 备注

✅ = 前端已实现，后端需对接  
⚠️ = 需要后端独立实现  
❌ = 暂不支持
