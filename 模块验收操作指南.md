# 知识星球问答小程序 - 深度场景验收操作指南 (V2)

# 重新初始化深度验收环境
npx tsx script/seed-full-acceptance.ts
本指南针对**深度业务场景**进行了扩展，涵盖了多孩管理、课时过期拦截、驳回反馈流及管理端即时配置等核心进阶功能。

## 准备阶段：环境初始化

在开始验收前，请确保后端服务正在运行。

1. **执行深度初始化脚本**：
   ```bash
   npx tsx backend/script/seed-full-acceptance.ts
   ```

2. **角色模拟窗口准备**：
   - **窗口 A (老师/管理员)**：Chrome 普通窗口。
   - **窗口 B (学生 1 - 子涵)**：Chrome 隐身窗口。
   - **窗口 C (学生 2 - 子凡 / 或过期号小明)**：第二个隐身窗口。
   - **窗口 D (家长)**：Edge 浏览器。

---

## 步骤一：多孩管理与数据隔离验收

1. **登录家长端 (身份 D)**：
   - 使用手机号 `13300000002` 登录。
2. **确认多孩列表**：
   - 点击“我的孩子”。
   - **验证点**：应能同时列出「子涵(老大)」和「子凡(老二)」。
3. **数据隔离验证**：
   - 分别查看两个孩子的问题列表。
   - **验证点**：确保子涵的问题不会出现在子凡的列表中，反之亦然。
   - **验证点**：子涵在【步骤二】中新提的问题，确认家长在子涵名下能即时看到，但在子凡名下看不到。

---

## 步骤二：驳回流程与反馈回路验收

1. **发起提问 (身份 B - 子涵)**：
   - 使用 `13300000001` 登录，发表一个标题为“求助物理公式”的问题。
2. **手动驳回 (身份 A - 老师)**：
   - 进入管理后台 -> 审核队列。
   - 找到子涵的物理题，点击 **「驳回」**。
   - **关键操作**：在理由框输入：“题目照片太模糊，请在光线充足处重拍”。
3. **查看驳回反馈 (身份 B - 子涵)**：
   - 收到 `audit_result` 通知。
   - 进入“我的提问”，查看该问题。
   - **验证点**：确认能清晰看到老师填写的驳回理由（红字展示）。
4. **家长同步监督 (身份 D - 家长)**：
   - 刷新家长端子涵的问题列表。
   - **验证点**：家长进入该已驳回的问题详情，应同样能看到驳回理由（验证了家长对孩子失败动态的知情权）。

---

## 步骤三：课时过期与权限拦截验收

1. **登录过期账号 (身份 C - 小明)**：
   - 使用 `13300000004` 登录。
2. **尝试提问**：
   - 点击首页“提问”按钮。
   - **预验证结果**：系统应直接弹出提示：“您的课时已到期，请联系老师续费”或跳转至提示页。
   - **验证点**：确认无法进入题目编辑页面（验证了 `validUntil` 的 Service 层硬拦截）。

---

## 步骤四：管理端配置即时生效验收

1. **老师端修改维度 (身份 A)**：
   - 进入后台的“题目维度配置”。
   - 新增一个选项或修改已有选项名称（例如将“公式法”改为“万能公式法”）。
2. **学生端即时刷新 (身份 B - 子涵)**：
   - 回到提问页面，点击刷新或重新进入。
   - **验证点**：下拉选择框中的内容应立即更新为“万能公式法”（验证了配置中心与前端的动态同步）。

---

## 总结

完成以上深度场景的验收，意味着系统不仅能跑通“正常流程”，还能在“权限失效”、“内容违规”、“多孩复杂关系”下保持逻辑自洽和数据隔离。这是生产环境发布前的最后一道防线。
