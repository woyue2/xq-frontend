# 需求背景

当前评论区已支持将“老师评论内容为`真棒`”渲染为按钮样式，但还有两个目标未完成：

1. 明确保证“非老师角色发送`真棒`不渲染为按钮”。
2. 为老师“真棒”按钮增加点击后“真棒”飘字上浮动画。

# 目标

在不重构、不新增依赖的前提下，最小改动完成以下结果：

1. 仅老师角色 + 评论内容精确为`真棒`时渲染按钮。
2. 点击该按钮立即触发上浮动画文本“真棒”。
3. 动画可在移动端正常可见，不影响现有评论提交/展示流程。

# 非目标

1. 不修改评论审核、存储、权限模型。
2. 不引入全局状态管理或复杂动画系统。
3. 不做额外 UI 优化与组件抽象。

