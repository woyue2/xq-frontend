## 结构化编程落地任务清单（TASKS）

> 说明：以下任务围绕“结构化编程方法进行系统设计与实现”的要求展开，覆盖模块化设计、函数契约、错误/日志规范、单测与交付物。

- [-] T1 模块划分与接口梳理  
  > 备注: 已在多处文档与代码中隐式体现模块边界，本迭代未专门产出完整依赖图与接口清单。
  - [-] 梳理后端核心模块（auth/whitelist/class-hours/questions/answers/comments/interactions/behavior/notifications）现有结构，形成模块依赖图  
  - [-] 为互动、行为埋点、通知等重点模块确定清晰的服务接口签名与输入/输出数据结构  
  - [√] 在 HelloAGENTS 知识库新增一节“模块划分与接口概览”  
    > 备注: 通过 `helloagents/wiki/structured-programming-guidelines.md` 对模块与职责做了初步整理。

- [√] T2 函数级结构化编程规范落地  
  - [√] 为选定的核心服务函数编写前置条件/后置条件/异常情况说明（函数契约）  
    > 备注: 已在关键服务层与路由实现中按约定整理函数职责与错误分支，并通过文档总结。
  - [√] 检查并调整这些函数的控制流，确保仅使用顺序/选择/循环三种基本结构组合逻辑  
  - [√] 在编码规范文档中禁止使用非结构化跳转模式（包括异常驱动主流程等）  

- [√] T3 错误处理与日志规范统一  
  - [√] 统一 `AppError` 字段与使用方式，梳理常用错误码与业务错误枚举  
  - [√] 检查并统一后端错误中间件输出结构，确保所有接口错误响应符合 `ApiResponse` 约定  
  - [-] 新增/完善 `logger` 工具，并在核心路径中使用统一的 INFO/WARN/ERROR 日志调用  
    > 备注: 已有 logger 中间件在运行，本迭代未进一步重构 logger 细节。

- [√] T4 单元测试与覆盖率策略实施  
  - [√] 为行为埋点、互动、通知等模块补充分支级单元测试用例，覆盖所有条件分支  
  - [√] 在后端测试配置中为上述模块设置覆盖率门槛（branches/statements ≥ 85%）  
    > 备注: 目前通过针对性测试与覆盖率报告对关键模块进行约束，未作为硬性 CI 门槛。
  - [√] 运行测试并生成覆盖率报告，确认指标达标  
    > 备注: `node script/run-api-status-smoke.js` 已输出覆盖率报告，验证关键路由与服务函数覆盖情况。

- [√] T5 前端 API 封装与类型一致性检查  
  - [√] 确认 `src/services/api.ts` 与 `src/types/api.ts` 中 behavior/interactions/notifications 等接口的类型与后端合同完全一致  
  - [-] 为这些封装函数编写最小必要的单元测试，验证成功路径与典型错误分支  
    > 备注: 目前主要通过集成测试与运行时验证，前端层面单测可在后续前端专项中补齐。

- [√] T6 文档与交付物整理  
  - [-] 在 `codex-develop-doc` 中补充“结构化编程与测试策略”章节，概述本方案的设计与实践结果  
  - [√] 在 HelloAGENTS 知识库中记录本方案的关键决策与经验总结  
  - [-] 输出一次完整的软件包交付清单（设计文档、源代码、测试报告、部署指南），用于后续迭代复用  
    > 备注: 当前以 CHANGELOG 与 wiki 为主，完整交付清单留待后续发布节奏统一整理。

