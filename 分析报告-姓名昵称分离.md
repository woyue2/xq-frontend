# 注册页面姓名与昵称分离 - 改动分析报告

## 一、现状分析

### 1.1 数据库现状
- **User表**（backend/prisma/schema.prisma）：
  - 当前只有一个 `nickname` 字段（String类型，非唯一）
  - 用于存储用户的显示名称
  - 没有专门的 `name`（真实姓名）字段

- **UserWhitelist表**：
  - 已有独立的 `name` 字段，用于白名单中的用户姓名
  - 与User表的nickname是分开的

### 1.2 后端API现状
- **注册接口**：`POST /auth/register`
  - 接收参数：`phone, code, nickname, grade, age, school, role, password`
  - nickname作为可选参数，未提供时生成默认值 `用户_${phone后4位}`
  
- **用户信息更新**：`PATCH /users/me`
  - 可更新字段：`nickname, avatar, grade, age, school`
  - 不支持更新姓名字段

- **返回用户信息**：
  - 所有接口返回的User对象都只包含 `nickname`，没有 `name`

### 1.3 前端现状
- **注册页面**（src/pages/LoginPage.tsx）：
  - 第363-375行：只有一个"姓名"输入框（实际对应nickname）
  - 标签为"姓名"，placeholder为"请输入姓名（用于展示的昵称，可选）"
  - 说明当前这个"姓名"实际是昵称的概念

- **类型定义**（src/types/index.ts）：
  - User接口只有 `nickname: string` 字段

- **个人资料页面**（src/pages/ProfilePage.tsx）：
  - 显示和编辑的都是nickname
  - 第136-164行：handleUpdateNickname只更新nickname

### 1.4 家长绑定场景（特殊处理）
- **家长注册并绑定孩子**：
  - 前端收集"孩子姓名"（childName）
  - 后端`parentService.bindChild`方法（第77-82行）将childName更新到孩子的`nickname`字段
  - 这是一个**需要调整**的地方：孩子姓名应该更新到`name`字段而非`nickname`

### 1.4 其他相关表
- **Question表**：有 `authorName` 字段（String），存储问题作者名称
- **Answer表**：有 `authorName` 字段
- **Comment表**：有 `authorName` 字段

这些表的authorName在创建时从User.nickname复制而来，用于历史记录展示。

---

## 二、改动影响评估

### 2.1 数据库层面（中等影响）
**需要改动：**
- ✅ 修改 `backend/prisma/schema.prisma`，在User模型中添加 `name` 字段
- ✅ 创建数据库迁移脚本
- ✅ 迁移后需要运行 `prisma migrate deploy`

**影响范围：**
- 仅User表增加一个可选字段
- 对现有数据无影响（字段可为空）
- 向后兼容，不影响现有功能

**改动大小：** ⭐⭐（较小）

### 2.2 后端层面（较大影响）

#### 2.2.1 类型定义改动
需要修改的文件：
1. `backend/src/types/api.ts`：
   - `RegisterPayload` 接口：添加 `name?: string`
   - 其他相关接口（如UpdateProfilePayload）也需要添加

2. `backend/src/services/auth.service.ts`：
   - `register()` 方法参数：添加 `name?: string`
   - 注册逻辑：处理name字段，存储到User.name
   - `login()` 和 `passwordLogin()` 返回的User对象需要包含name
   - 内存降级用户对象也需要包含name

3. `backend/src/routes/auth.routes.ts`：
   - 第72-81行：从req.body解构时添加 `name`
   - 第82-91行：传递给authService.register时添加 `name`

4. `backend/src/routes/user-me.routes.ts`：
   - 允许更新的字段列表（第76行）需要添加 `name`
   - 更新逻辑需要处理name字段

5. `backend/src/services/user.service.ts`：
   - `updateProfile()` 方法参数：添加 `name?: string`
   - 数据校验和AI审核逻辑需要处理name字段

6. `backend/src/config/database.ts` 或相关文件：
   - 确保Prisma Client重新生成以包含新字段

#### 2.2.2 业务逻辑考虑
- **注册时name和nickname的关系**：
  - 方案A：name必填，nickname可选（未填则使用name作为nickname）
  - 方案B：name可选，nickname必填（未填则生成默认nickname，name可为空）
  - 方案C：两者都可选，但有业务规则（如至少填一个）

  建议采用**方案B**，保持向后兼容：
  - nickname用于社区展示，必填（或自动生成）
  - name用于真实身份标识，可选填
  - 如果用户未提供nickname，仍按现有逻辑生成默认值

- **白名单预填**（可选增强）：
  - 注册时如果手机号在白名单中，可以用白名单的name预填姓名字段
  - 但这需要前端配合，可作为二期优化

#### 2.2.3 影响范围统计
- 直接修改文件：约 **8-10个** 后端文件
- 间接影响：所有调用User对象的地方理论上都应该返回name字段
- 测试文件：所有创建User的测试都需要更新（约20+个测试文件）

**改动大小：** ⭐⭐⭐⭐（较大）

### 2.3 前端层面（中等影响）

#### 2.3.1 类型定义改动
1. `src/types/index.ts`：
   - User接口：添加 `name?: string`

2. `src/types/api.ts`：
   - RegisterPayload：添加 `name?: string`
   - UpdateProfilePayload（如有）：添加 `name?: string`

#### 2.3.2 注册页面改动
文件：`src/pages/LoginPage.tsx`

**需要修改：**
1. 状态管理（第35-36行）：
   ```typescript
   const [name, setName] = useState(''); // 新增姓名字段
   const [nickname, setNickname] = useState('');
   ```

2. 表单UI（第363-375行后）：
   - 将现有"姓名"输入框改为"昵称"输入框
   - 新增"真实姓名"输入框（可选）
   - 调整布局顺序：建议先姓名后昵称，或根据业务需求

3. 表单验证（第108-247行 handleSubmit）：
   - 根据业务规则添加name/nickname的验证逻辑
   - 如果name选填，则无需验证；如果必填，需要添加验证

4. 提交参数（第212-221行）：
   ```typescript
   await authService.register({
     phone,
     code,
     password,
     name: name.trim() || undefined,      // 新增
     nickname: nickname.trim() || `用户${phone.slice(-4)}`,
     grade: isStudentInvite ? grade : undefined,
     age: isStudentInvite ? parseInt(age, 10) : undefined,
     school: isStudentInvite ? school : undefined,
     role: desiredRole
   });
   ```

#### 2.3.3 家长绑定场景改动
文件：`src/pages/LoginPage.tsx`

**需要修改：**
1. 第44行：`childName`状态变量名可以改为`childRealName`（可选）
2. 第475行：标签"孩子姓名"改为"孩子真实姓名"
3. 第478行：placeholder改为"请输入孩子真实姓名"
4. 第661行：setBindName状态设置（如果改名则同步修改）

**说明：** 这个改动是为了与后端`parentService.bindChild`的逻辑保持一致，将孩子姓名更新到`name`字段而非`nickname`。

#### 2.3.4 个人资料页面改动
文件：`src/pages/ProfilePage.tsx`

**可选改动：**
- 显示姓名（如果已填写）
- 编辑姓名功能（类似编辑昵称）
- 如果决定不提供编辑姓名功能，则至少要在UI上显示姓名

**最小改动方案：**
- 在个人信息区域显示姓名（如果有）
- 不添加编辑功能（姓名通常不常修改，可通过白名单或后台管理修改）

#### 2.3.5 其他前端文件
- `src/services/api.ts`：authService.register的TypeScript类型定义需要更新
- 任何使用User类型的地方，理论上都可以显示name字段（非强制）

#### 2.3.6 影响范围统计
- 直接修改文件：约 **5-7个** 前端文件（增加1-2个）
- 测试文件：需要更新相关测试（约5-10个）

**改动大小：** ⭐⭐⭐（中等）

### 2.4 测试层面（中等影响）
- 后端单元测试和集成测试：约20+个文件需要更新测试数据
- 前端集成测试：需要更新注册流程测试
- E2E测试（如有）：需要更新

**改动大小：** ⭐⭐⭐（中等）

### 2.5 总体影响评估

| 层面 | 影响程度 | 文件数量 | 复杂度 |
|------|---------|---------|--------|
| 数据库 | ⭐⭐ | 1-2 | 低 |
| 后端 | ⭐⭐⭐⭐ | 8-10+ | 高 |
| 前端 | ⭐⭐⭐ | 4-6 | 中 |
| 测试 | ⭐⭐⭐ | 20+ | 中 |
| **总计** | **⭐⭐⭐⭐** | **30-40+** | **高** |

**总体评价：**
这是一个**中等偏大**的改动，涉及全栈多个模块。但由于是新增字段（而非修改现有字段），**向后兼容性良好**，可以分阶段实施，风险可控。

---

## 三、实施方案

### 3.1 阶段一：数据库迁移（后端）
1. 修改 `backend/prisma/schema.prisma`：
   ```prisma
   model User {
     id         String   @id @default(cuid())
     phone      String   @unique
     name       String?  // 新增：真实姓名，可选
     nickname   String
     avatar     String?
     // ... 其他字段
   }
   ```

2. 生成迁移文件：
   ```bash
   cd backend
   npx prisma migrate dev --name add_name_to_user
   ```

3. 提交迁移文件到版本控制

### 3.2 阶段二：后端代码更新
1. 更新Prisma Client：
   ```bash
   cd backend
   npx prisma generate
   ```

2. 修改类型定义文件：
   - `src/types/api.ts`：更新RegisterPayload等接口
   - 如有必要，创建统一的User类型定义

3. 修改注册相关代码：
   - `src/routes/auth.routes.ts`：接收name参数
   - `src/services/auth.service.ts`：处理name字段
     - register方法参数添加name
     - 创建User时设置name字段
     - 返回User对象包含name

4. 修改用户更新相关代码：
   - `src/routes/user-me.routes.ts`：允许更新name
   - `src/services/user.service.ts`：处理name更新和审核

5. 修改所有返回User对象的接口（可选但建议）：
   - 确保所有User响应都包含name字段（可为null）

6. 更新测试数据：
   - 批量更新所有测试文件中的User创建数据
   - 可以添加name字段或保持为空

### 3.3 阶段三：前端代码更新
1. 更新类型定义：
   - `src/types/index.ts`：User接口添加 `name?: string`
   - `src/types/api.ts`：RegisterPayload添加 `name?: string`

2. 修改注册页面：
   - `src/pages/LoginPage.tsx`：
     - 添加name状态
     - 修改UI：将现有"姓名"标签改为"昵称"，新增"真实姓名"输入框
     - 调整验证逻辑
     - 更新提交参数

3. 修改API服务类型：
   - `src/services/api.ts`：authService.register的泛型类型自动推断，无需修改

4. 更新个人资料页面（可选）：
   - `src/pages/ProfilePage.tsx`：
     - 显示姓名（如果存在）
     - 可添加编辑姓名的对话框（类似昵称编辑）

5. 更新其他使用User类型的地方（非强制）：
   - 任何显示用户信息的地方，如果有空间可以显示姓名

### 3.4 阶段四：测试更新
1. 后端测试：
   - 更新所有创建User的测试数据，添加name字段（可为空）
   - 更新注册相关测试，传入name参数
   - 运行测试确保通过

2. 前端测试：
   - 更新注册页面测试，验证新字段
   - 更新API测试，验证name字段传递

3. 集成测试：
   - 运行完整的注册流程测试
   - 验证用户创建后name字段正确存储

### 3.5 阶段五：数据迁移（生产环境）
**注意**：如果生产环境已有数据，需要处理历史用户

**方案A（推荐）**：保持历史数据不变
- 现有用户的nickname保持不变
- name字段为NULL
- 用户后续可以补充姓名

**方案B**：数据迁移脚本（如果需要）
- 将现有所有User的nickname复制到name字段
- 然后nickname保持不变（或清空让用户重新填写）
- 需要编写一次性脚本

**建议采用方案A**，因为：
1. 改动最小，风险最低
2. 现有nickname可能并不是真实姓名
3. 用户可以后续自愿填写真实姓名

### 3.6 阶段六：部署与验证
1. 本地开发环境测试
2. 测试环境部署验证
3. 生产环境灰度发布（如有）
4. 监控相关日志和错误

---

## 四、风险与注意事项

### 4.1 风险
1. **数据库迁移失败**：需要备份数据库，准备回滚方案
2. **测试覆盖不全**：可能遗漏某些使用nickname的地方
3. **前后端类型不一致**：需要严格TypeScript类型检查
4. **历史数据问题**：authorName等历史记录不受影响，但新记录会使用nickname

### 4.2 注意事项
1. **向后兼容**：
   - name字段应为可选（`String?`）
   - 注册接口的name参数应为可选
   - 避免破坏现有客户端

2. **AI审核**：
   - name字段也需要经过AI审核（参考nickname的审核逻辑）
   - 更新`backend/src/services/ai-audit.service.ts`中的内容类型

3. **白名单关联**（可选增强）：
   - 注册时如果手机号在白名单中，可以用白名单的name预填
   - 需要修改`auth.service.ts`的register方法

4. **用户引导**：
   - 前端可以提示用户填写真实姓名（用于学校管理等）
   - 但不要强制必填，避免影响注册转化率

5. **隐私保护**：
   - 姓名属于敏感信息，确保API权限控制
   - 不要在不必要的接口返回name字段（如公开信息）

---

## 五、工作量估算

| 任务 | 预计工时 | 备注 |
|------|---------|------|
| 数据库迁移 | 0.5天 | 包括生成迁移、测试 |
| 后端类型定义更新 | 0.5天 | 接口、DTO等 |
| 后端业务逻辑修改 | 1.5-2天 | 注册、更新、返回值处理、家长绑定 |
| 后端测试更新 | 1天 | 约20+测试文件 |
| 前端类型定义更新 | 0.5天 | |
| 注册页面UI修改 | 1天 | 包括表单、验证 |
| 家长绑定UI调整 | 0.25天 | 修改childName标签和提示 |
| 个人资料页面修改 | 0.5天 | 可选 |
| 前端测试更新 | 0.5天 | |
| 集成测试与调试 | 1天 | |
| **总计** | **6-8天** | 一人工作量 |

**说明**：如果有多人协作，可以缩短到4-5天。

---

## 六、建议的实施策略

### 6.1 分步实施
1. **第一步**：仅实现数据库和基础类型，不修改前端UI
2. **第二步**：后端支持name参数，但前端暂不收集
3. **第三步**：前端添加姓名字段，完成完整功能

这样可以逐步验证，降低风险。

### 6.2 功能开关（可选）
添加环境变量控制是否启用name字段：
```typescript
const USE_SEPARATE_NAME = process.env.USE_SEPARATE_NAME === 'true';
```
便于快速回滚。

### 6.3 监控指标
- 注册成功率（前后对比）
- 用户填写姓名的比例
- 姓名审核通过率

---

## 七、结论

**是否要改动数据库？** ✅ **是**，必须在User表添加`name`字段。

**改动有多大？** 
- 涉及**30-40+个文件**的修改
- 预计**5-7人天**的工作量
- 影响范围覆盖**数据库、后端、前端、测试**
- 但由于是**新增字段**，**向后兼容**，**风险可控**

**建议：**
1. 如果业务确实需要区分真实姓名和昵称，这个改动是必要的
2. 建议采用**最小化改动方案**，name字段可选，不强制必填
3. 分阶段实施，先完成后端和数据库，再逐步完善前端
4. 充分测试，特别是注册流程和用户更新流程

---

## 八、详细修改清单

见下节《详细修改清单》。
